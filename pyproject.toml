[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simslink"
version = "0.1.0"
description = "Desktop mod manager for The Sims 4 on Linux, with automated crash diagnosis"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "watchdog>=4.0",
    "python-dotenv>=1.0",
    "requests>=2.31",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "pywebview>=5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.14",
    "httpx>=0.27",
]

[project.scripts]
simslink = "desktop:main"

[tool.hatch.build.targets.wheel]
packages = ["."]
include = ["*.py", "backend/*.py", "frontend/**/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
