# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Project

**SimsLink** — a desktop mod manager for The Sims 4 on Linux: a FastAPI backend serving a local REST API, an HTML/CSS/JS frontend, both wrapped in a native window via pywebview, with a local SQLite database. There is no official CurseForge client for Linux; this project fills that gap. Its main differentiator is automated crash diagnosis: parsing the game's `lastException.txt` to identify which installed mod likely caused a crash.

Reference project (not a dependency, just prior art worth checking for ideas): [SimsForge](https://github.com/Teyk0o/simsforge) — different stack (TS/Express/Prisma), notable for its filesystem import flow and malicious-mod flagging.

## Language convention — read this first

- **All code, comments, docstrings, commit messages, and the README are written in English.** No exceptions.
- **Only the UI-facing strings are localized** (French and English, see `frontend/i18n/`). Never hardcode UI text — always go through the i18n layer (loaded client-side and injected into the DOM).
- Any French terms appearing in project notes or specs are for human discussion only and must never leak into source code.

## Tech stack

- Python 3.11+
- FastAPI — local REST API (`localhost:8000`) hosting all business logic (DB, scanner, mod_manager, crash_analyzer, package_parser, curseforge client).
- pywebview — wraps the frontend in a real native desktop window, not a browser tab. Linux system deps: `python3-gi` + `gir1.2-webkit2-4.0` (WebKitGTK).
- Frontend: HTML/CSS/JS (vanilla by default; React only if a view's component complexity actually justifies it) — talks to the backend via `fetch`.
- SQLite (local DB, no server component)
- `watchdog` for filesystem monitoring (mod folder + download folder)
- `concurrent.futures` / `multiprocessing` for parallelized hashing during full scans

Single language (Python) for all business logic — no multi-language split like a Tauri/Rust stack would require. Backend and frontend are two processes/layers within the same app, not a client/server product.

## Critical game constraints — never violate these

The Sims 4 loads mods from `Documents/Electronic Arts/The Sims 4/Mods/` with two different depth rules depending on file type. Getting this wrong silently breaks mods for the user with no error message, so it must be enforced in code, not just documented:

- **`.package` files**: loaded via full recursive scan — any depth under `Mods/` works.
- **`.ts4script` files**: loaded only at the root of `Mods/` **or exactly one level deep**. Anything nested further is silently ignored by the game.
- **Resulting install rule**: every mod gets its own top-level subfolder under `Mods/<mod_id>/`. Never install directly at the `Mods/` root (name collisions across mods), never nest deeper than one level (breaks `.ts4script` loading).
- `Mods/` itself stays flat. Rich organization (categories, browsing) lives in the library folder, not in the game folder.

## Architecture

```
backend/
  main.py              # FastAPI app + routes — thin HTTP layer over the modules below
  config.py             # .env loading
  db.py                 # SQLite schema + migrations
  curseforge.py         # CurseForge API client — Direct Mode only, requires CURSEFORGE_API_KEY
  curseforge_match.py    # links a loose import to its real CurseForge entry via file-fingerprint matching
  download_watcher.py   # watches the download folder — Assisted Mode install/update path
  mod_manager.py         # install / enable / disable / delete / update pipeline
  dependencies.py         # dependency graph resolution, incl. translation-mod detection
  package_parser.py       # DBPF (.package) header reader — resource listing, STBL comparison
  scanner.py               # incremental scan (metadata) + on-demand full scan (hashing)
  crash_analyzer.py        # lastException.txt parsing + automated bisection
  cache_cleaner.py         # cache cleanup targets
  conflict_detector.py     # duplicate/conflict detection across installed mods
  game_options.py          # reads options.ini for settings SimsLink checks (Script Mods Allowed)
  profiles.py              # named, switchable sets of "which mods should be active"; also missing-mod reminders (record_missing_mod_if_saved())
  blacklist.py             # local, user-editable list of known-bad mod name/id patterns
  logging_config.py        # configures the "backend" logger — console + file, level from LOG_LEVEL
  backups.py               # shared timestamped-backup helper under LIBRARY_DIR/.backups/
  broken_mods.py           # unmanaged Mods/ folders + manually-rezipped tracked mods — detection and safe fixes
  path_settings.py         # editable SIMS4_GAME_DIR/SIMS4_USER_DIR/LIBRARY_DIR (Settings), with coherence checks + migration
  loose_mods.py            # adopts .package/.ts4script files loose at Mods/ root; confirmable merge-suggestion tool
frontend/
  index.html
  style.css
  app.js               # or src/ if a view's complexity earns it a React tree
  i18n/
    fr.json
    en.json            # UI strings only, loaded and injected client-side
desktop.py              # launches FastAPI (background thread) + opens the pywebview window
```

`backend/*.py` (everything except `main.py`) is plain business logic, importable and testable on its own — `main.py` exposes it over HTTP, and the frontend calls those routes. Cross-module imports within `backend/` use relative imports (`from . import mod_manager`, `from .config import Config`, ...), since they're all part of the same package. When adding a feature, business logic goes in the relevant `backend/` module; `main.py` stays a thin routing layer over it, not a place to grow feature logic of its own.

### Library + symlink model

- **Library**: the real storage location for installed mods (flat structure — no need for multi-level categorization here either; that idea was explicitly dropped).
- **`Mods/<mod_id>/`**: a symlink (or a copy, as a fallback if the filesystem doesn't support symlinks) pointing into the library.
- Enabling/disabling a mod = creating/removing the symlink. Fast, reversible, no file movement.

## Environment variables (`.env`)

```
SIMS4_GAME_DIR=...        # game install directory
SIMS4_USER_DIR=...        # .../Documents/Electronic Arts/The Sims 4 (Mods/ is always this dir's own "Mods" subfolder — no separate setting for it)
LIBRARY_DIR=...           # app-managed mod library. Optional — defaults to ~/.SimsLink/library
CURSEFORGE_API_KEY=...    # optional — see Direct Mode / Assisted Mode below
DOWNLOAD_WATCH_DIR=...    # watched download folder for Assisted Mode, default ~/Downloads
GAME_VERSION=...          # auto-detected if possible, otherwise manual
BACKUP_RETENTION_COUNT=... # optional, default 5 — backups kept per mod under LIBRARY_DIR/.backups/
MODS_WATCHER_ENABLED=...   # optional, default true — real-time watcher for external changes under Mods/
LOG_LEVEL=...              # optional, default INFO — DEBUG/INFO/WARNING/ERROR/CRITICAL
```

Everything SimsLink itself owns and doesn't get an explicit override for lives under `~/.SimsLink/` by default — the app's database and log (`~/.SimsLink/simslink.sqlite3`, `~/.SimsLink/simslink.log`, previously under the XDG data dir) and, unless `LIBRARY_DIR` says otherwise, the mod library and its `.backups/` (`~/.SimsLink/library/`). One conventional dotfolder rather than spreading state across XDG base directories or requiring `LIBRARY_DIR` to always be set.

`SIMS4_GAME_DIR`/`SIMS4_USER_DIR`/`LIBRARY_DIR` are only the *bootstrap* values now — see `backend/path_settings.py` below for how Settings can change all three afterward without editing `.env`.

## Direct Mode vs Assisted Mode

This is the most important behavioral fork in the whole app, and it's not a future nice-to-have — it's how the app must be built from day one, since API key approval isn't a prerequisite for starting development.

A persistent pill (topbar, `#modePill`) always shows the active mode — a short label plus a green/red indicator gem (`.gem`, default `--accent` green, `.warn` class switches it to `--warn` red-orange), with the fuller sentence as its hover tooltip:

| Mode | Condition | Pill label (visible) | Tooltip |
|---|---|---|---|
| Direct Mode | `CURSEFORGE_API_KEY` set and valid | "CurseForge Connected" | "Direct mode: Connected to CurseForge." |
| Assisted Mode | No key, or invalid/expired key | "CurseForge Disconnected" | "Assisted mode: Disconnected from CurseForge." |

**Direct Mode**: full catalog browsing, compatibility badges, metadata (description/screenshots) from the API, automatic update detection via `curseforge.py`.

**Assisted Mode** (must work with zero API access):
1. User clicks through to a mod's CurseForge page, opened in the system's default browser (not inside the app's pywebview window) — ordinary human browsing, not automated access, so no ToS concerns.
2. `download_watcher.py` watches `DOWNLOAD_WATCH_DIR` and detects new `.zip`/`.package`/`.ts4script` files.
3. User confirms the detected file is the intended mod.
4. File goes through the standard install pipeline (extraction, `.ts4script` depth check, placement under `Mods/<mod_id>/`, DB entry) — identical to Direct Mode from this point on.
5. The mod's CurseForge URL is still stored locally for later manual re-checking.
6. Updates work the same way: no automatic version comparison is possible, so the Updates view becomes a manual checklist with "Check on CurseForge" links; a new download for an already-installed mod is detected and offered as a replacement (with backup) via the same watcher.

**What's unavailable in Assisted Mode** (must be clearly surfaced in the UI, never silently degraded): automatic "update available" badges, changelogs, pre-download compatibility checks against `game_version_min/max`.

**Fully mode-independent**: library, symlinks, local dependency tracking, Crash Mode, cache cleanup, enable/disable/delete, incremental scanning.

Do not gate core functionality behind having an API key. `curseforge.py` should be the only module that hard-requires `CURSEFORGE_API_KEY`.

## Startup scan — must never block the UI

A full recursive hash scan on every launch is a real cost on large libraries (real-world example: 20k+ `.package` files). Implement accordingly:

- On launch, the frontend renders the library immediately from a DB-backed route (`GET /api/mods`) — no blocking "scanning..." screen. The backend must not make that route wait on a scan.
- `backend/main.py`'s `create_app()` builds (but never starts — see below) `app.state.mods_watcher` (a `scanner.ModsFolderWatcher`) and exposes `app.state.run_startup_scan` (adopts anything dropped directly under `Mods/` via `scanner.import_untracked_mods()`, then catches up on size/`mtime` changes via `scanner.incremental_scan()` — this combo is `run_mods_rescan()`, a single shared closure). `desktop.py` runs `run_startup_scan` on a background thread right after the server comes up — it never delays opening the pywebview window.
- `mods_watcher`'s `on_change` fires on every raw filesystem event with no debounce of its own; `create_app()` wraps it in a 2-second debounce (`schedule_mods_rescan`, backed by a `threading.Timer`) before triggering another **`run_mods_rescan()`** — the same combined import-then-catch-up call the startup scan uses, not just `incremental_scan()` alone — so a burst of events (e.g. one symlink toggle touching several paths) costs one rescan, not several, *and* a real folder dropped in while the app is already running (a manual copy/duplicate from outside SimsLink, say) gets adopted without needing a full restart. (**Regression, fixed 2026-08-22**: before this, only the one-time startup scan called `import_untracked_mods()` — the watcher's own debounced rescan re-hashed already-tracked mods but never discovered new ones, so a folder dropped in mid-session silently stayed invisible until the next full app restart; see `tests/test_backend_main.py::test_regression_schedule_mods_rescan_also_imports_untracked_mods`.) `desktop.py` calls `app.state.mods_watcher.start()`/`.stop()` around the pywebview window's lifecycle, same pattern as `download_watcher` — but only if `config.mods_watcher_enabled` (env var `MODS_WATCHER_ENABLED`, default on); watchdog's `Observer.stop()`/`.join()` raises on a watcher that was never `.start()`ed, so that guard has to match on both ends, not just skip the start call. Read-only in Settings, same as every other env-driven value.
- Full hash scan is manual-only: `POST /api/settings/full-scan` (synchronous — the Settings "Full scan" button shows its own "scanning..." state and waits), parallelized across cores via `scanner.full_scan()`'s `ProcessPoolExecutor` since hashing is CPU-bound. Also calls `import_untracked_mods()` first (same regression/fix as above — "full scan" should mean full, not "only what's already tracked") and reports the count as `mods_imported` alongside the existing `mods_scanned`/`files_hashed`/`files_removed` stats.
- Like `download_watcher` (see "Assisted Mode download detection" below), none of this runs as a side effect of `create_app()` itself — building the app stays free of background threads so tests aren't affected; only `desktop.py` (or a test calling `app.state.run_startup_scan()`/`app.state.schedule_mods_rescan()` directly) triggers it.
- **What still requires a real restart**: nothing, now — `import_untracked_mods()` runs on every debounced watcher rescan and every manual full scan, not just at launch. What it still *won't* catch, restart or not: a bare `.zip` file sitting loose directly at the `Mods/` root (only zips found *inside* a real, unmanaged folder count — see `broken_mods.py`), and anything dropped *inside* an already-managed mod's own folder (that folder is a symlink, and `broken_mods.py`/`import_untracked_mods()` both skip symlinks outright — a stray file there isn't a new mod or a broken one, it's just noise inside a tracked mod's real library folder, out of scope for either scanner). A loose `.package`/`.ts4script` **file** (not a folder, not a zip) directly at `Mods/` root is a *different*, separately-handled case — see `backend/loose_mods.py` below — deliberately manual-only (a Settings action), never folded into this automatic rescan.

## Database schema (SQLite)

```sql
mods (
  id, curseforge_id, name, author, category,
  library_path, primary_type,          -- 'package' | 'script' | 'mixed'
  installed_version, latest_version,
  game_version_min, game_version_max,
  compat_status,                       -- 'compatible' | 'incompatible' | 'unknown'
  third_party_distribution_allowed,    -- bool, from CurseForge API
  active, install_date, update_date,
  thumbnail_url, thumbnail_local,
  short_description, full_description,
  screenshots JSON,
  links JSON,                          -- curseforge_url, author_site, donation, etc.
  is_loose_import,                     -- bool, set by loose_mods.py's import_loose_files() — see below
  namespace_override                   -- manual correction for the Library's inferred grouping label — see below
)

mod_files (
  id, mod_id, relative_path, hash, extension
)

dependencies (
  id, mod_id, depends_on_curseforge_id,
  dependency_type,     -- 'required' | 'optional' | 'translation'
  confidence,           -- 'confirmed' | 'suggested' (for auto-detected translations)
  mandatory
)

profiles (id, name)
profile_mods (profile_id, mod_id)

crash_log (
  id, date,
  raw_last_exception TEXT,
  auto_suspect_mods JSON,      -- mods identified via traceback parsing
  active_mods_snapshot JSON,
  bisection_in_progress BOOLEAN,
  bisection_history JSON,      -- tested batches + results
  confirmed_faulty_mod_id,
  user_note
)

blacklist (
  id, pattern,          -- matched case-insensitively as a substring of a mod's name or id
  note,
  created_date
)

missing_mods (
  id, mod_id,            -- the deleted mod's former id — no FK to mods(id), the whole point is to outlive that row
  name, curseforge_url,  -- captured at deletion time, before profile_mods' ON DELETE CASCADE erases every trace
  source_profile_names,  -- denormalized text, e.g. "Weekend Build, Main Save" — not a live FK to profiles either
  detected_date
)

settings (
  key, value              -- generic key/value overrides; so far only sims4_game_dir/sims4_user_dir/library_dir (path_settings.py)
)
```

## Key features and how they should behave

### Crash Mode
- Parse `lastException.txt` only. **Never parse `lastCrash.txt`** — it's confirmed unreadable/unusable even by the community, don't waste effort on it.
- Cross-reference traceback `File "..."` lines against `mod_files` paths under `Mods/` to identify suspect mods directly.
- Fall back to regex pattern matching for known error signatures (e.g. broken imports of outdated shared libraries like sims4communitylib/ts4lib) when no mod path appears directly in the trace.
- **Never auto-suggest deletion from a single occurrence** — an isolated LastException can be benign and unrelated to any mod. Surface suspects with confidence level, let the user decide.
- If no clear suspect: offer automated bisection (binary search via symlink toggling, batch-based, user confirms after each relaunch, logarithmic convergence). Never require full file moves — always via the symlink layer.
- **Regression, fixed 2026-08-21**: `lastException.txt` was assumed to be a single raw traceback (per the unvalidated note that used to sit here); real files confirmed it's XML (`<root><report>...<desyncdata>traceback</desyncdata>...</report>...</root>`) that can bundle several unrelated occurrences accumulated over one play session. `crash_analyzer.parse_reports()` now splits each `<desyncdata>` out and `record_crash_reports()` stores one `crash_log` row per occurrence, so unrelated incidents' suspects never get merged into a single row — falls back to treating the whole input as one occurrence for plain text (a format change, or this module's own non-XML test fixtures). `POST /api/crash/analyze` returns `{"found", "reports": [{"crash_log_id", "suspects"}, ...]}` (was a single `crash_log_id`/`suspects` pair) — Crash Mode's frontend now renders one block per occurrence, each with its own suspects/bisection-start action. See `tests/test_crash_analyzer.py`'s `parse_reports`/`record_crash_reports` tests (built from real, GUID-redacted captured crash data) and CLAUDE.md's "Path-matching heuristic" note in `crash_analyzer.py`'s module docstring, also updated now that it's validated against real data.

### Cache cleanup
Target these specifically, with the reasoning baked into any confirmation dialog:
- `localthumbcache.package` — delete the file (regenerates; stale entries after mod changes can cause invalid lookups and crashes)
- `cache/` — clear contents, keep the folder and any `FileCache.cfg`/`.ini`
- `cachestr/` — clear contents, keep the folder
- `cachewebkit/` — delete if present (only exists while game is running)
- `onlinethumbnailcache/` — delete if present
- `localsimtexturecache.package` — delete if present
- **Never touch**: saves, tray files, screenshots, `options.ini`, `resource.cfg`.
- **Never include** `lastException.txt`/`lastCrash.txt` in this cleanup — they're managed separately by Crash Mode so diagnostic history isn't lost before analysis.
- Suggest cleanup automatically after install/update/disable actions, but always require confirmation before deleting. **Landed 2026-08-21**: `suggestCacheCleanup()` in `app.js`, wired into `installFromCatalog()`, `resolvePendingDownload()` (install/replace, not dismiss), `applyUpdate()`/`doUpdateAll()` (at least one success), and `toggleActive()` — but only on the disable path, not enable (not in the spec list above, and a newly-active mod isn't what would've left stale entries behind). Reuses the exact same `/api/cache/targets` + `openConfirmModal()` + `/api/cache/clean` flow as Crash Mode's manual "Clear cache" button (`buildCacheTargetNodes()` factored out so both share the same target list rendering) — no new backend route. Stays silent (no modal, no error banner) if the target list is empty or can't be fetched, since this is a courtesy suggestion riding along an action the user already took, not something that should itself surface an error.

### Translation-mod detection
No dedicated "translation" relation type exists in the CurseForge API (only `embeddedLibrary` / `incompatible` / `optionalDependency` / `requiredDependency`), so this always needs multiple weak signals combined, never a single automatic match:
1. Parse mod description for translation keywords (multi-language) + CurseForge URLs pointing at an already-installed mod — strongest signal.
2. Name/slug heuristics (`[FR]`, `- French Translation`, `_VF`) — weak, pre-filter only.
3. `.package` binary analysis via `package_parser.py`: check the file is STBL-only (no `.ts4script`, unusually small), compare STBL Group ID/Instance ID against the candidate source mod — strong confirmation, but on-demand only, never a full-library scan.
- **Every suggested link requires user confirmation.** Store `confidence` as `confirmed` or `suggested` — never silently create a `translation` dependency.
- **Surfaced 2026-08-21**: `POST /api/mods/{mod_id}/detect-translation` (runs `detect_translation_signals()` for one mod, on-demand — never a full-library scan) and `POST /api/mods/{mod_id}/suggest-translation` (creates the `confidence='suggested'` link — never `'confirmed'` directly), plus generic `POST /api/dependencies/{id}/confirm` and `.../reject` for any suggested dependency, not just translations. Frontend: the Library detail panel's Dependencies section gets Confirm/Reject buttons on `suggested` rows, and a "Check for translation match" button that shows detected signals (grouped by source mod, so a mod matching both the name-heuristic and STBL signals doesn't offer two separate "Link" buttons for the same source) with a "Link" action per group. `mod_manager.enable()` still separately blocks on unresolved `required` dependencies, unrelated to this UI.
- **Regression caught building this**: `dependencies._read_packages()` called `package_parser.read_package()` with no error handling — a candidate `.package` that isn't valid DBPF (corrupted download, non-standard file, or just test fixture data) crashed the whole `stbl_signal()`/`detect_translation_signals()` call instead of being treated as inconclusive for that one file. Now catches `package_parser.DbpfError` per file and skips it; see `tests/test_dependencies.py::test_regression_stbl_signal_skips_unparseable_package_instead_of_raising`.

### Dependency resolution
Track `required` / `optional` / `translation` dependency types. Block install/enable only on unresolved `required` dependencies; warn but don't block on `optional`.

### Duplicate/conflict detection
`backend/conflict_detector.py`, surfaced via `GET /api/conflicts` and a dismissible-by-collapsing (not deletable) banner in the Library view. Purely informational — same "suspicion is not confirmation" rule as Crash Mode: never blocks install/enable, never suggests deleting anything.
- **Exact duplicate mods** (strongest signal): two or more mods whose *entire* file sets are byte-identical (same set of `mod_files.hash` values, not just one shared file) — "shares 100% of its files with another mod." `find_exact_duplicate_mods()` groups active mods by the frozenset of their file hashes; 2+ mods landing on the same set are reported together. Suppresses every weaker signal below for any pair it covers.
- **`.package` duplicates**: two or more mods with a byte-identical file (same `mod_files.hash`) — usually the same mod installed twice under different names. Detected via a `GROUP BY hash HAVING COUNT(DISTINCT mod_id) > 1` query — no new file parsing, `hash` is already computed at install time.
- **`.ts4script` name collisions**: two or more mods shipping a script file with the same filename at their mod root. Since `.ts4script` archives are Python zipimport archives and the interpreter's module cache keys on module name (not path), a same-named script in two mods — often each bundling their own copy of a shared library — can silently shadow one another. Detected the same way, grouping on `relative_path` instead of `hash`.
- These signals come entirely from data already in `mod_files`; nothing here re-reads file contents or re-parses `.package` headers.
- Also surfaced in the Library warnings banner (same collapsible list, not a separate one): `blacklist.py` matches (below) — the banner's toggle count and list combine both.

### "Script Mods Allowed" check
`backend/game_options.py` reads the game's `options.ini` (location: `SIMS4_USER_DIR/options.ini`) looking for a `scriptmodsenabled` key — **not** tied to a specific `[Section]`, since the game's ini format/section naming isn't officially documented and a wrong guess there would make the check silently never find anything; it scans every `key=value` line regardless of section. Returns `True`/`False` only for an unambiguous value, `None` (never treated as "disabled") when the file or key is missing. Exposed via `GET /api/status`'s `script_mods_allowed` field, re-read on every call (cheap file read) rather than cached like `direct_mode`, since the game can rewrite this file while SimsLink is running. Shown as an always-expanded warning banner in the Library view when `false`, plus a read-only line in Settings.

**Regression, fixed 2026-08-21**: the key was originally guessed as `scriptmodsallowed` (matching the setting's in-game display name), which never matches a real `options.ini` — the actual key the game writes is `scriptmodsenabled`. This left the check permanently returning `None` ("unknown") against real game data despite the setting being present; see `tests/test_game_options.py::test_regression_key_is_scriptmodsenabled_not_scriptmodsallowed`.

### Mod profiles ("saved states")
`backend/profiles.py`, using the `profiles`/`profile_mods` tables (previously defined in the schema but never used by any code until now). A profile is a named, timestamped snapshot of "which mods should be active" — the UI frames this as "Save"/"Load" rather than "profile", but it's the same underlying feature; `created_date` (added in a later migration, nullable for profiles that predate it) records when it was saved, `datetime.now(timezone.utc).isoformat()` same as `mods.install_date`. `set_profile_mods()` replaces membership wholesale (simpler for a UI that captures "the mods active right now" than one managing per-mod add/remove). `activate_profile()` ("load") is **additive-only**: it re-enables exactly the saved mods, but never disables a mod that isn't part of the snapshot — an earlier version also disabled every non-member, which meant loading an older save would silently disable a mod installed since (e.g. extracting an archive through `broken_mods.py`'s repair action, then loading a save from before that existed — the newly installed mod would vanish from the active set with no indication why, reported as "the load feature does nothing"). Still fails fast (propagates `UnresolvedRequiredDependencyError`/`ModManagerError`) on the first mod that can't be enabled, and silently skips a member mod_id that's since been deleted, rather than raising — in practice this is unreachable under normal operation, since `profile_mods.mod_id` has `ON DELETE CASCADE`: the instant a mod is deleted, every profile's membership record of it vanishes immediately, so a stale member can never actually reach this check. See `missing_mods`/`record_missing_mod_if_saved()` below for how that same cascade is turned into a feature instead of a silent data loss. Creation flow is deliberately minimal: "save the mods active right now under a name" (name + current active set) rather than a full mod-picker/editor — editing membership later means deleting and recreating. `list_profiles()` sorts most-recently-saved first, not alphabetically, matching the "saved states" framing. UI: two icons on the Library page (`#saveStateBtn`, `#loadStateBtn`, left of the "Simplified names"/"Group by author" switches) — not a Settings section — since there's rarely more than a couple of saved states to pick from; Save opens a name-prompt confirm modal, Load opens a small dropdown (`#loadStateDropdown`) listing existing saves with per-row load/delete icon actions.

### Local mod blacklist
`backend/blacklist.py` — a simplified version of SimsForge's malicious-mod flagging (see this file's "Project" section). Purely local and manual: entries are typed in by the user via Settings, nothing fetches a shared/remote list, and a match only ever informs (surfaced in the Library warnings banner alongside conflict-detector's findings) — it never blocks install/enable, same "suspicion is not confirmation" rule as everywhere else. Matching is a case-insensitive substring check against both a mod's display name and its id/slug.

## Logging

`backend/logging_config.py`'s `configure_logging(config)` sets up the `"backend"` logger (every module gets `logging.getLogger(__name__)`, which propagates up to it) with two handlers: console (stderr) and a file at `config.log_path` (`<XDG data dir>/simslink.log`, next to the DB). Level comes from `LOG_LEVEL` (env var, default `INFO`, one of `DEBUG`/`INFO`/`WARNING`/`ERROR`/`CRITICAL` — invalid values raise `ConfigError`). `desktop.py` calls it once at startup and also passes `config.log_level` to `uvicorn.Config(log_level=...)`, so the one setting controls uvicorn's access logging too, not just ours.

Like `download_watcher`/`mods_watcher`/`run_startup_scan`, this is **never called from `create_app()`** — only `desktop.py` (or a test passing an explicit `log_path` override, the same pattern `create_app(db_path=...)` uses) calls `configure_logging()`, so building an app during a test run never reconfigures global logging state or writes into the real XDG log file.

What actually gets logged, by design (a level setting that logged nothing would be cosmetic, not a real feature):
- **DEBUG**: every HTTP request/response via a FastAPI middleware (`method path -> status (Xms)`) — verbose tracing, not meant to be read directly.
- **INFO**: user-meaningful mutating actions — mod enabled/disabled/deleted/installed (catalog or update-applied)/profile activated/cache cleared, and which mode (Direct/Assisted) the app started in.
- **WARNING**: notable-but-expected conditions — an enable blocked by an unresolved required dependency, a configured `CURSEFORGE_API_KEY` that got rejected, a mod confirmed faulty after crash bisection.
- Truly unexpected exceptions (a real 500) aren't specially logged here — uvicorn already logs those tracebacks on its own.

Log level is read-only in Settings (`GET /api/settings`'s `log_level`/`log_path` fields), same as every other env-driven setting — not a live runtime toggle.

## Things to never do

- Never install a mod directly at the `Mods/` root, and never nest a `.ts4script` more than one folder deep.
- Never make an automated request to any CurseForge endpoint without a valid API key — this includes CDN downloads, which require a key as of July 16, 2026.
- Never scrape CurseForge pages. It's explicitly prohibited by the platform's Terms of Use and won't even solve the download problem once the CDN requires a key.
- Never silently auto-delete a mod based on crash analysis. Suspicion is not confirmation.
- Never hardcode a UI string outside the `frontend/i18n/` layer.
- Never block on a full filesystem hash scan — not the FastAPI request handling the initial page/data load, and not the frontend's render while waiting for one.
- Never gate a mode-independent feature (library, Crash Mode, cache cleanup, etc.) behind `CURSEFORGE_API_KEY` being set.

## Testing

Unit tests and regression tests are mandatory for this project, not optional. Every module listed under Architecture should have corresponding tests, and every bug fix should come with a regression test that would have caught it.

- Framework: `pytest`.
- Test files mirror the module structure: `tests/test_<module>.py` for each file under `backend/` (e.g. `tests/test_mod_manager.py`, `tests/test_scanner.py`) — business-logic modules are tested directly, independent of the HTTP layer.
- `backend/main.py`'s routes get their own tests in `tests/test_backend_main.py`, via FastAPI's `TestClient` — no real HTTP server, no browser needed. A route test should stay thin: assert the route calls the right module function with the right arguments and translates its result/errors into the right HTTP status/JSON, not re-test business logic already covered at the module level. `create_app(config, db_path=...)` takes an explicit `db_path` for exactly this reason — it lets tests point the app at an isolated tmp_path database instead of `config.db_path`'s real, fixed XDG location.
- Frontend (`frontend/app.js` or `src/`): no test framework mandated by default given the project's current size — plain vanilla JS driving `fetch` calls against already-tested routes doesn't carry much regression risk on its own. Revisit this (e.g. Vitest + React Testing Library) if/when a view's client-side state logic grows non-trivial enough that a bug there wouldn't be caught by backend tests.
- **Priority modules for thorough coverage** — these encode the game's undocumented behavior and are the easiest place for silent regressions to slip in:
  - `mod_manager.py`: the `.ts4script` depth rule and the `Mods/<mod_id>/` placement rule must have explicit tests that assert installation fails/corrects itself when a script mod would land deeper than one level, and that `.package` files at any depth are accepted.
  - `scanner.py`: incremental scan must have a test proving an unchanged mod folder triggers zero hash computation, and a test proving a changed `mtime`/size does trigger one.
  - `crash_analyzer.py`: traceback parsing needs fixture `lastException.txt` samples (both "mod path directly in trace" and "core game code only" cases) with known expected suspect output. Also test that a single occurrence never produces an auto-delete suggestion.
  - `dependencies.py`: translation-detection heuristics need fixtures per signal type (description match, name heuristic, STBL comparison) with assertions on the resulting `confidence` value — a `suggested` link must never silently become `confirmed` without explicit user action in the code path.
  - `package_parser.py`: DBPF header parsing needs at least one real `.package` fixture (a small STBL-only file and a mixed-resource file) to catch parser regressions against the binary format.
- **Mode-dependent code** (`curseforge.py`, `download_watcher.py`, and `backend/main.py`'s Direct-Mode-only routes): tests must run without a real API key or network access — mock the CurseForge API client entirely (`tests/test_backend_main.py` does this via `monkeypatch.setattr("backend.main.curseforge.CurseForgeClient", ...)`), and mock filesystem events for the download watcher (no real file I/O against a live Downloads folder in CI).
- **Regression tests**: whenever a bug is fixed, add a test reproducing the original failure before the fix, named to reference the issue (e.g. `test_regression_ts4script_nested_two_levels`).
- Run the full suite before considering any feature "done" — this applies to Claude Code's own workflow in this repo, not just to human review.

## Development workflow

Once the above testing expectations are met, follow the collaboration pattern this project already uses: freeze acceptance criteria before implementing, change one variable at a time when debugging platform-specific behavior (symlinks, filesystem quirks), and treat a passing test suite as the GO signal before moving to the next feature — not as an afterthought once everything "looks done."

## Current project status

- **FastAPI + pywebview is the app, as of 2026-08-21.** The prior Flet UI (`ui/*.py`, root `main.py`, `i18n/`) has been removed — `backend/` + `frontend/` + `desktop.py` (`simslink` script entry) is the only implementation now, covering all five views (Library, Catalog, Updates, Crash Mode, Settings). Visual design follows `simslink-mockup.html`. All business-logic modules physically moved under `backend/` in the same change (cross-module imports are now relative — `from . import mod_manager`, etc.); watch for this if grepping old commits/notes for bare `import mod_manager`-style imports, which no longer resolve.
- **Assisted Mode download detection — closed 2026-08-21.** `backend/main.py`'s `create_app()` builds a `download_watcher.DownloadWatcher` and a `PendingDownloadStore` (in-memory, lock-protected — written to by the watcher's background thread via `report_download()`, read/drained by request threads) but does not start the watcher itself, so building the app has no filesystem side effect; `desktop.py` calls `app.state.download_watcher.start()`/`.stop()` around the pywebview window's lifecycle. Routes: `GET /api/downloads/pending`, `POST /api/downloads/{token}/install`, `POST /api/downloads/{token}/replace`, `POST /api/downloads/{token}/dismiss`. The frontend polls `/api/downloads/pending` every 3s (`startDownloadPolling()` in `app.js`, no SSE/WebSocket — plain polling is fine for a single-user local app, no sub-second latency need) and shows `#downloadOverlay` when something's waiting, mirroring the old Flet dialog's Install/Replace/Dismiss choices. Tests call `app.state.report_download(path)` directly to simulate a detection with no real watcher thread involved.
- **`Mods/` real-time watcher + manual full scan — closed 2026-08-21.** See "Startup scan" above for the full wiring (`app.state.mods_watcher`, `run_startup_scan`, `schedule_mods_rescan`, `POST /api/settings/full-scan`).
- **Reviewing/confirming suggested translation dependencies — landed 2026-08-21.** See "Translation-mod detection" above for the routes and the `dependencies.py` bug fix that came with it.
- **Duplicate/conflict detection — landed 2026-08-21.** See "Duplicate/conflict detection" above. Of brief §7's cross-cutting feature list, this is the only one built so far; the rest (`options.ini` "Script Mods Allowed" check, one-click mod profiles using the already-defined but unused `profiles`/`profile_mods` tables, a batch "update all" action, a local mod blacklist) remain net-new, unstarted work, not gaps in something already built.
- **Backup retention — landed 2026-08-21.** `download_watcher.py` created a timestamped backup on every replace but never purged old ones — `LIBRARY_DIR/.backups/` grew forever. Fixed: new `Config.backup_retention_count` (env var `BACKUP_RETENTION_COUNT`, default 5, validated as a positive int in `from_env()`) + a purge step, keeping only the newest N backups per mod_id (purge is scoped per mod — replacing one mod never touches another's backup history). Exposed read-only via `GET /api/settings` and the Settings view's new "Backups" section; still no in-app editing (same as the other settings), just `.env`. Of brief §6.8's settings table, this is the only entry actually backed by real logic so far — the rest (theme, tile size, log level, pattern-sensitivity, etc.) still need the underlying subsystem built before there's anything to expose. **Extracted into `backend/backups.py` on 2026-08-21** (`backup_folder()`/`purge_old_backups()`) so `broken_mods.py`'s automatic fixes (see below) reuse the exact same mechanism instead of duplicating it — `download_watcher.confirm_replace()` calls it the same way it always did, just from the new module.
- The README's "automatic (confirmable) detection of translation mods" claim is now accurate (see above). Its batch "Update all" claim is now accurate too — see below.
- **Batch "Update all" — landed 2026-08-21.** Frontend-only; no new backend route needed, since `doUpdateAll()` in `app.js` just loops `POST /api/updates/{mod_id}/apply` over whatever `POST /api/updates/check` most recently flagged as `update_available` (tracked in `state.updatableMods`). One mod's failure doesn't abort the batch — failures are collected and reported together (`updates.update_all_partial_error`), then the check re-runs so applied mods drop off the list and failures are re-offered. The "Update all" button only appears once there are ≥2 updatable mods (with exactly one, the per-row "Update" button is already a single click). Confirmation goes through the shared `openConfirmModal()`, listing every mod about to be updated before it starts — each individual `apply` still backs up via `download_watcher.confirm_replace()` same as before, nothing new there.
- **Brief §7 "Script Mods Allowed"/profiles/blacklist, and brief §6.8 theme/tile size — landed 2026-08-21.** See "Script Mods Allowed" check / "Mod profiles" / "Local mod blacklist" above for the backend side. Frontend: Settings gets Theme (dark/light) and Library tile size (large/compact) dropdowns — both pure client-side preferences (`localStorage`, no backend route), applied via a `data-theme` attribute and a `.tile-compact` class respectively; the theme is applied pre-paint by a tiny inline `<script>` in `index.html`'s `<head>` (reads `localStorage` before the stylesheet/DOM even render) specifically to avoid a dark→light flash on load. Settings also gets Profiles and Blacklist management sections (list/create/activate/delete, list/add/remove).
- **Log level — landed 2026-08-21.** See "Logging" above for the full design. Deliberately scoped to just this half of brief §6.8's pair — **Crash Mode "pattern-sensitivity" is still explicitly not attempted**: `crash_analyzer.py`'s known-pattern matching is a binary regex check with no notion of a tunable score/threshold to attach a slider to, and — candidly noted in that module's own docstring — the traceback-parsing path has never been validated against a real `lastException.txt` (none was available during development, only synthetic fixtures). Tuning "sensitivity" on an unvalidated heuristic would mean guessing at a false-positive/false-negative tradeoff with no real crash-log data to check it against; revisit once real logs are available to validate the detector itself, not just add a control to it.
- **Automatic cache-cleanup suggestion — landed 2026-08-21.** See "Cache cleanup" above. This closed a real behavioral gap (not an aspirational one): CLAUDE.md/the brief had specified this from the start, but nothing implemented it before now — cache cleanup was manual-only via Crash Mode.
- **Real-time `Mods/` watcher toggle — landed 2026-08-21.** `MODS_WATCHER_ENABLED` env var (see "Startup scan" above), read-only in Settings like every other env-driven setting — not a live runtime toggle. Closes brief §6.8's "activer/désactiver le watcher temps réel" line.
- **Regression caught by the backend/ move, now fixed**: `config.py`'s `DEFAULT_ENV_PATH` was `Path(__file__).resolve().parent / ".env"`, which — once `config.py` lives in `backend/` instead of the project root — silently resolved to `backend/.env` instead of the real `.env`. Fixed to `.parent.parent`; see `tests/test_config.py::test_regression_default_env_path_resolves_to_project_root_not_backend_dir`. Worth remembering as a category: any other `Path(__file__)`-relative path assumption written before the move should be double-checked the same way.
- **A `CURSEFORGE_API_KEY` was added and verified working — 2026-08-23.** The app now genuinely runs in Direct Mode on this machine (`verify_key()` succeeds, confirmed in the startup log: "Starting in Direct Mode") — `curseforge.py` is no longer purely speculative/unexercised code as its own module docstring still says (that note is now stale for this environment, though the caveat still holds for anyone without a key). Assisted Mode remains fully supported and is still what CI/tests exercise (mocked, no real key) — this doesn't change the "build for Assisted Mode first" default, just that Direct Mode has now actually been exercised at least once, for real.
- `brief-sims4-mod-manager.md` (the detailed spec this file summarizes) was updated 2026-08-21 for the FastAPI/pywebview stack (architecture tree, Assisted Mode's install mechanics, i18n file paths, view/section naming) and hasn't been touched since — most of what it once flagged as aspirational (batch "update all", §7's duplicate detection/Script Mods Allowed/profiles/blacklist) has since landed and isn't reflected there. Two real gaps remain against §7: no rollback/restore UI for the backups `download_watcher.py` already creates (the files are there under `.backups/`, nothing surfaces them to restore from), and no changelog display on updates (CurseForge's API doesn't expose one per-file, so this isn't buildable without that data existing upstream). §6.8's full settings table (log level, pattern-sensitivity, live-editable folders) also remains open — see above. This file (`CLAUDE.md`) stays authoritative wherever the brief disagrees.
- **`GAME_VERSION` auto-detection — landed 2026-08-21.** `.env`'s comment always said "auto-detected if possible, otherwise manual", but nothing implemented the auto-detection half until now — `config.game_version` was purely a manual env var. `game_options.detect_game_version()` reads the installed build string (e.g. `1.126.78.1020`) straight out of `Game/Bin/TS4_x64.exe`'s embedded Windows PE VERSIONINFO resource (`ProductVersion`), via the new `pefile` dependency (pure Python, pip-installable, no system package needed) — works against a Wine/Proton-installed `.exe` since it's just reading raw bytes, no Windows API involved. Falls back to `TS4_DX9_x64.exe` if the primary exe is missing. `Config.from_env()` calls it only when `GAME_VERSION` is blank (a deferred `from . import game_options` inside the method body, since `game_options.py` already imports `Config` from this module — a module-level import the other way would cycle).
- **Broken-mod-folder detection + safe auto-fix — landed 2026-08-21.** New `backend/broken_mods.py`, same informational, non-blocking spirit as `conflict_detector.py`: `scan_broken_mods()` finds folders directly under `Mods/` with nothing the game would actually load (a `.package`/`.ts4script` search finds none) and classifies why — `empty`, `unextracted_archive` (a `.zip` was dropped in but never extracted), `unpacked_script` (a `.ts4script` was extracted in place instead of kept intact — the game only loads the archive itself), or `unrecognized` (files present, cause unclear — e.g. a leftover log, or non-mod data like a stray save file). Surfaced via `GET /api/mods/broken`, folded into the Library warnings banner alongside conflicts/blacklist matches (same collapsible list). `fix_broken_mod()` auto-fixes only the two safe reasons (`empty` → delete; `unextracted_archive` with exactly one zip → install it through the normal pipeline) via `POST /api/mods/broken/{name}/fix`, gated behind the frontend's confirm modal same as every other destructive action — never automatic. Always backs up the original folder first (via the newly-shared `backups.py`, see above) so a bad auto-fix is recoverable. `unpacked_script`/`unrecognized` have no fix button: reconstructing a `.ts4script` from loose files left after a bad extraction is a real capability gap, deliberately deferred as riskier (loose files may be incomplete or reordered relative to the original archive) — revisit as a separate iteration once there's a clearer picture of what's actually recoverable.
- **Desktop shell temporarily browser-only, not pywebview — since 2026-08-21.** `desktop.py` currently opens SimsLink in the system's default browser (`webbrowser.open()`) instead of a pywebview native window. Root cause not yet found: on this dev machine (GTK3 WebKit2GTK under a native Wayland session), a pywebview window rendered correctly and accepted keyboard input, but real mouse clicks never reached the page (confirmed via WebKit inspector: hit-testing found the correct DOM element under the cursor, no overlay/z-index issue, no JS errors) — reproduced under both the native Wayland GDK backend and `GDK_BACKEND=x11` (XWayland), so it isn't a simple backend-selection fix. Nothing about `backend/`'s routes or `frontend/`'s JS is pywebview-specific — the same app serves either shell unmodified — so this is purely a `desktop.py` entry-point concern. Revisit pywebview once the click-forwarding bug is root-caused; until then, don't assume a native window is running when debugging UI issues on this machine.
- **Library UI polish pass — landed 2026-08-21.** Several frontend-only changes, no backend routes added: (1) a grid/list view toggle for the Library (`state.viewMode`, persisted like theme/tile size); (2) mods with an active conflict/blacklist match sort to the top of both views, then by author (when known) then name, with a "Resolve" button inline in the warnings banner jumping straight to that mod's detail comparison; (3) mods are grouped under an author heading (a name + horizontal rule) when `author` is known — repeats per contiguous run, so the problem/non-problem split can produce the same author's heading twice; (4) a name-prefix like `[SS]`/`(JBABS)` is split off the displayed name and shown as the tile's abbreviation instead, purely a rendering change — `mod.name` in the DB is untouched; (5) a delete (trash) icon directly on every card/row, reusing the same confirm-modal delete flow as the detail panel; (6) `primary_type` now surfaces as one or two badges (CC for package content, Script for script content — a `mixed` mod gets both, not a single "mixed" label); (7) the **Catalog** nav entry is hidden entirely in Assisted Mode (it has zero content without a CurseForge key — just an empty notice); **Updates stays visible**, since its manual checklist (linked mods + "Check on CurseForge" links) still has real content without a key. A global `[hidden] { display: none !important; }` rule (added the same day) is what makes toggling `.hidden`/`element.hidden` on any of these actually work — several component classes (`.btn`, `.card`, `.warning-banner`, ...) set their own `display`, which silently defeated the browser's default `[hidden]` rule at equal specificity before that fix.
- **Best-effort `unpacked_script` repair — landed 2026-08-21.** Closes the "no fix button" gap the broken-mod-folder entry above explicitly deferred, but only for this one reason, and only as a best-effort attempt clearly labeled as such — not promoted into `FIXABLE_REASONS`/`fix_broken_mod()`, which stays reserved for the two fixes safe enough to be silent about the risk. New `broken_mods.attempt_script_repair()`: backs up the folder (same `backups.py` mechanism), re-zips its contents as-is (relative paths preserved, archive root = folder root) into a `.ts4script`, and installs it through the normal `mod_manager.install()` pipeline — this can only ever be as good as the original extraction; a partial or reordered extraction still "installs" but won't load in-game, which is exactly why it's a separate opt-in action and not folded into the automatic fixes. Route: `POST /api/mods/broken/{name}/attempt-script-repair`. Frontend: `unpacked_script` folders render as a real card/row via `brokenFolderPseudoMod()` (a synthetic mod-shaped object with `__brokenFolder` set), which `buildCard()`/`buildListRow()` branch on internally — no separate builder functions. A wrench icon opens the standard confirm modal (explicitly warning the attempt can fail silently) before calling the new route. Removed from the banner's own list in the same change (`renderWarnings()` filters `reason !== "unpacked_script"` out) since it would otherwise be double-surfaced.
- **Broken-mod cards unified with real mod cards, equal card heights, badge consolidation — landed 2026-08-21.** Follow-up to the entry above: broken folders no longer get special-cased top-of-grid placement — `render()` folds their pseudo-mods into the same `visibleMods()` array before sorting/grouping, so they sort, group under an author header (when their name matches one via the same heuristics as any other mod), and get their version extracted exactly like a real mod (`groupingAuthor()`/`splitNameVersion()` don't know or care that it's a pseudo-mod). They stay visually distinct — red (`--danger`/`--danger-glow`, a true red, not the orange `--warn` used for suspected conflicts, since a broken folder is confirmed non-functional, not merely suspected) with a broken/alert icon (`BROKEN_ICON_SVG`) instead of the folder icon it briefly had. Also: every classification pill a card can carry (type CC/Script, duplicate, broken, version, compat, disabled) now renders through one shared `buildBadges()` — previously the grid card scattered these across three separate spots (top-left disabled-tag, top-right compat-badge, bottom-left type-badges, plus version as plain text in the meta row); now they're one row (`.card-badges`, overlaid on the fixed-height thumbnail so badge count never affects card height) mirroring what the list view's `.list-mod-row-badges` already did. Equal-height grid cards: CSS Grid's default row-stretch only equalizes cards within the same row, not across the whole grid, and `-webkit-line-clamp` alone only caps the *maximum* lines — a short one-line title/description still shrank the card. Fixed by giving `.card-body h3`/`p` an explicit `min-height` matching their line-clamp (2 lines), so every card's natural height is identical regardless of content, before grid stretch even needs to apply.
- **Disabled-mod styling, "Simplified names" toggle, `unextracted_archive` promoted to a problem card — landed 2026-08-21.** Three more frontend-only follow-ups: (1) a disabled mod's title gets `text-decoration: line-through` in both views, and the list view's `.is-inactive` row gains `filter: grayscale(0.7)` on top of its existing opacity dimming — opacity alone still let badge/gem colors read as normal at 60%, which didn't look distinctly "off" the way the grid card's own grayscale thumb filter already did. (2) A "Simplified names" checkbox (`#simplifiedNamesCheckbox`, persisted like theme/tile size) sits next to the grid/list view switch in a new `.head-controls` wrapper; unticking it makes `buildCard()`/`buildListRow()` show `mod.name` raw instead of `groupingAuthor()`'s cleaned-up `displayName` (see `titleText()`) — deliberately scoped to just the title text: author-header grouping, sort order, the avatar label, and the version badge all keep using `groupingAuthor()` regardless, since those are organizational metadata, not "the mod's name" as far as this toggle goes. (3) `unextracted_archive` folders (not just `unpacked_script`) now also get the red pseudo-mod card treatment via `visibleBrokenModFolders()` (renamed from the script-only `visibleBrokenScriptFolders()`) — `buildBrokenActionButton()` dispatches the card's repair icon to the right route per reason (`attempt-script-repair` vs. the existing safe `fix` route), and returns no button at all for an archive with 2+ zips (ambiguous, no safe single-click fix) so the card still flags it as a problem without offering an action guaranteed to fail — mirroring the restraint the warnings banner's own "Fix" button already had. `empty`/`unrecognized` still stay banner-only.
- **Version badge normalization, delete on broken folders, square cards, 4-tier sort fix — landed 2026-08-21.** (1) `formatVersionBadge()` prefixes a bare version ("6.9.4") with "v" for display, leaving one that already has it ("v1.7"/`installed_version` values that already include it) untouched. (2) New `broken_mods.delete_broken_folder()` (backs up, then removes — works for any reason, unlike the reason-specific fix/repair actions) behind `DELETE /api/mods/broken/{name}`; every broken card/row now gets a delete icon alongside its reason-specific repair action — broken folders have no enable/disable concept (no symlink/active state to toggle, and nothing in them loads anyway) so delete is their only action beyond repair. (3) `.card` now uses `aspect-ratio: 1/1` instead of a fixed-height thumb + intrinsic-height body: `.thumb` is a `flex-basis` percentage of the card instead of a fixed px height, `.card-body` gets `min-height: 0` so it shrinks to the remainder, and the grid's minimum column width went from 210px to 230px to give the now-square card enough absolute room for its title/description/meta content — verified via computed-style measurement (`getBoundingClientRect()` width == height, zero `.card-body` scroll overflow, action buttons fully inside the card's bounds) rather than assuming the numbers work out. (4) The sort comparator's old two-step (active-first, then problem-first) let a broken pseudo-mod and a real conflict-flagged mod tie and fall through to alphabetical — since both were stuffed into the same `problems` Set. Replaced with `modTier()`, a single 4-value ranking (0 broken / 1 problem / 2 normal / 3 disabled) applied within each author group, matching the intended priority order exactly.
- **"Unknown author" header for the authorless bucket — landed 2026-08-21.** Mods with no real `author`, no bracket-prefix, and no inferred series match already sorted to the end of the Library grid/list (author-presence is the top sort key), but ran header-less off the end of the last real author's section — now they get their own generic header (`library.unknown_author_header`, "Auteur inconnu"/"Unknown author") in `render()`'s grouping loop, same contiguous-run logic as any other author.
- **Exact duplicate mods (100% file match) + author-based tagging — landed 2026-08-22.** New strongest conflict signal, `conflict_detector.find_exact_duplicate_mods()`: two or more active mods whose *entire* file-hash set is identical (not just one shared file, which is `duplicate_package`'s weaker signal) are grouped as `exact_duplicate_mod` and suppress every weaker signal for that pair, same precedence pattern `folder_duplication` already used. `GET /api/conflicts` now also resolves each mod's `author` (previously just name/active/install_date) so the frontend can decide which side of an exact-duplicate pair to blame — `duplicateTagModIds()`: when one member has a known author and another doesn't, the unauthored one gets the "Duplicate" tag (presumed the redundant re-download/rehost). When there's no known-vs-unknown split to go on — nobody has an author, or everybody does, whether the same author twice or two different ones — `identicalContentTagModIds()` tags every member with a neutral "Identical content"/"Contenu identique" pill instead, deliberately *not* picking a side: guessing which of two different authors' mods is "the copy" from arbitrary metadata (install date, etc.) risks a wrong, unfounded accusation baked into the UI, the same "suspicion is not confirmation" reasoning applied everywhere else in this app (blacklist matches, translation-dependency suggestions). Both tags still make the pair a "problem" tier in the sort order and get the red card border either way — only the tag text/color differs.
- **Broken mods openable in the detail panel, state-colored detail hero, collapsible author groups, Yes/No switches — landed 2026-08-22.** (1) A broken-folder card/row is clickable again — `openBrokenDetail()`/`renderBrokenDetail()` populate the same `#detail` panel from the folder object already in hand (no `/api/mods/{id}` row exists for it), hiding the dependencies/conflicts sections and swapping the standard Open-folder/Delete buttons for a `#dBrokenActions` container (hidden rather than replaced, so a later real-mod detail view can just unhide the originals instead of needing to rebuild them — the earlier version of this destructively cleared `#dActions`, which would have broken every subsequent real-mod detail open). (2) `.detail-hero` now reflects the viewed mod's state, matching the card border language: red (`is-broken`) for a broken folder, orange (`is-problem`, via a new `isProblemMod()` check against `state.conflicts`/`state.blacklistMatches`) for a suspected conflict, the original green gradient otherwise. (3) Clicking an `.author-group-header` toggles `state.collapsedAuthors` (session-only, not persisted) and re-renders — a collapsed group keeps its header (so it can be re-expanded) but skips every member card/row; only meaningful when grouping is on (see next). (4) Two behavioral changes to the head controls: the "Simplified names" checkbox became a Yes/No segmented switch (`.switch-control`, shared by both switches below, styled like the existing grid/list `.view-toggle`), and a new "Group by author" Yes/No switch (`state.groupByAuthor`, persisted like the others) — off, `render()` sorts by tier+name only, with no author headers and nothing to collapse.
- **"Zip" category for unextracted archives, unified duplicate wording, duplicate_package demoted off the grid — landed 2026-08-22.** (1) `unextracted_archive` is no longer visually lumped in with `unpacked_script`'s red "Cassé"/"Broken" treatment — `brokenStateClass(folder)` picks `is-archive` (a new purple `--purple`/`--purple-glow` theme: card/row border+background, badge, detail hero) over `is-broken` for that one reason, badged "Zip" instead, and its action button shows a distinct unzip/box icon (`UNZIP_ICON_SVG`, `.unzip-icon-btn`, purple) instead of the wrench used for the script-repair action. Nothing about the underlying fix logic changed, purely a display category so a "this needs unzipping" folder doesn't read as alarming as a genuinely broken one. (2) The neutral "Identical content" tag (two exact-duplicate mods with no known-vs-unknown author split to blame one side) was renamed to just "Doublon"/"Duplicate" — same word as the accusatory tag now, the two are still visually distinct only by color (`warn-badge` orange when one side is blamed, plain/neutral when neither is). (3) `duplicate_package` (a mod sharing only *some* of its files with another — the weakest overlap signal) no longer counts toward `problemModIds()`, so it no longer red-borders a card or sorts it into the "problem" tier; it's still fully visible in that mod's own detail panel (`renderDetailConflicts()` isn't filtered by kind), just not flagged grid-wide anymore. `folder_duplication` and `exact_duplicate_mod` are unaffected and still count. Also fixed a real pre-existing bug found while touching this code: the "Duplicate"/"Cassé" badge modifier classes (`.cc-badge.warn-badge`/`.cc-badge.danger-badge`) were scoped to `.cc-badge` only, so every list-view `.pill` version of the same badge silently rendered with no color at all — the modifiers are now standalone so they apply to either base class.
- **Multi-archive extraction choice, delete for "unrecognized" folders, a real path bug fixed along the way — landed 2026-08-22.** (1) `BrokenModFolder.zip_names` (bare filenames) is now `zip_paths` (paths relative to the folder, posix-style) — `_classify()` scans recursively, so a zip anywhere but the folder's own root previously resolved to the wrong location entirely (`entry / zip_names[0]` looked for it directly under `entry`) with no test ever exercising that case. (2) New `broken_mods.extract_selected_zips(name, config, conn, zip_paths)` behind `POST /api/mods/broken/{name}/extract-zips`: the real-world case `fix_broken_mod()` never handled — a single download bundling several archives the user has to choose between (mutually exclusive variants) or all needs (required pieces split apart) — "often we have to choose one or several." Every selected archive installs as its own mod (disambiguated by the archive's own filename stem when more than one is picked); the whole original folder, including anything left unselected, is backed up before any extraction and removed only once every selection has been handled. `fix_broken_mod()`'s own single-archive path now delegates to this instead of duplicating the backup/install/cleanup logic. Frontend: `brokenActionSpec()` now always offers an unzip action for `unextracted_archive` — one archive keeps the direct one-click confirm, two or more open `confirmExtractZips()`, a checkbox-per-archive picker built via `openConfirmModal()`'s existing `extraNodes` slot (first archive pre-checked as a default, not a recommendation). (3) "Another level of zip" (an archive that, once extracted, turns out to contain only further archives — no `.package`/`.ts4script` of its own): rather than bespoke recursion, that archive's contents are extracted into a fresh folder directly under `Mods/` and reported under the response's `"deferred"` list (folder names, not mod ids) — the next `scan_broken_mods()` picks it up as its own new `unextracted_archive` entry, so choosing what to extract just runs again one level deeper through the same UI. (4) The warnings banner's "unrecognized" rows (e.g. a stray per-save data file, a leftover log from a resource a mod's own readme says is now redundant) get a manual delete button (`confirmDeleteBrokenFolder()`, same as the card treatment) — previously only 'empty' had any button at all there.
- **Profiles timestamped and reframed as "Save state"/"Load" — landed 2026-08-22.** Migration 5 adds `profiles.created_date` (nullable, same `datetime.now(timezone.utc).isoformat()` convention as `mods.install_date`), set by `create_profile()` and returned by every route that already resolved a `Profile` (`profile_dict()` in main.py — no new routes needed, the full save/load/delete/rename-membership CRUD already existed). `list_profiles()` now orders most-recently-saved first instead of alphabetically. Frontend: the Settings section and its two buttons — already functionally "save current state" / "load a saved state" before this, just labeled around the word "profile" — now read that way ("Saved states" heading, "Save current state"/"Load" button labels, each row showing its mod count and a `formatSavedAt()` date+time stamp) — see "Mod profiles" above for why this didn't need a parallel feature built alongside the existing one.
- **Default `~/.SimsLink/` data location, `SIMS4_MODS_DIR` retired, light theme removed, "unmanaged folders" banner simplified — landed 2026-08-22.** (1) `LIBRARY_DIR` (and, unless it's overridden, `DEFAULT_DATA_DIR` — the DB and log) now default to under `~/.SimsLink/` instead of requiring `LIBRARY_DIR` to always be set and spreading state across the XDG data dir — see "Environment variables" above; `REQUIRED_VARS` shrinks to just `SIMS4_GAME_DIR`/`SIMS4_USER_DIR`. (2) `SIMS4_MODS_DIR` is no longer read from the environment at all — `Config.from_env()` always derives it as `sims4_user_dir / "Mods"` (a stale value left in an old `.env` is silently ignored rather than risking disagreement with `SIMS4_USER_DIR`); the `Config` dataclass itself initially kept a plain `sims4_mods_dir` field for this (unchanged for every test/script constructing one directly, only `from_env()`'s sourcing changed) — **that field was removed outright on 2026-08-22** once it became clear storing it at all was the redundant part, not just how it was sourced: `sims4_mods_dir` is now a `@property` on `Config` deriving `self.sims4_user_dir / "Mods"` on every access, so there's no longer a second value that could ever disagree with `sims4_user_dir` in the first place. Every direct `Config(...)` construction (only `tests/conftest.py`'s `app_config` fixture and `from_env()` itself did this) simply stopped passing `sims4_mods_dir=` — reading `config.sims4_mods_dir` anywhere else is unaffected, a property reads exactly like the field it replaced. (3) The light theme (`:root[data-theme="light"]`, the Settings dropdown, the pre-paint inline `<script>` in `index.html`'s `<head>`) is gone entirely — dark is now the only theme, no toggle. (4) The Library warnings banner (broken_mods.py's `empty`/`unrecognized` folders — `unpacked_script`/`unextracted_archive` already moved to their own cards, see above) is now "Fichiers à nettoyer"/"{count} file(s) to clean up" instead of "unmanaged mod folder(s) found"; each row drops the "Contenu non reconnu"-style kind label and full description sentence in favor of just the file/folder name, and the old per-reason "Fix" text button is gone too (an empty folder's "fix" was always just "delete it", so the single generic trash-icon `buildDeleteBrokenButton()` — shared with the card treatment — covers both without a redundant second button).
- **`~/.SimsLink/` migration incident, same day: an existing install's real DB got orphaned — fixed with an explicit one-time migration.** The default-location change above (env vars entry) moved `DEFAULT_DATA_DIR` with no migration path, so an existing user's real database (at the old XDG-based location) was silently left behind and `create_app()` started against a brand-new *empty* one at the new path instead — every installed mod disappeared from the Library with no error, while broken-folder detection kept working (it reads `Mods/` directly, independent of the DB), which is what made the symptom read as "my library location must be wrong" rather than "the DB moved." Fixed with `config.migrate_legacy_data_dir()`: moves the old db/log into the new location, but only when the old one has real mod rows and the new one doesn't already have its own (never overwrites real data in either direction). Deliberately **not** called from `Config.from_env()`/`create_app()` — both are exercised directly by tests, which must never touch a real `~/.SimsLink` or XDG data dir — only `desktop.py` calls it, once, before `create_app()`. Also applied directly to recover the developer machine's own orphaned data (`.local/share/simslink/` → `~/.SimsLink/`) when this was caught.
- **Save/Load relocated to Library-page icons, activate_profile() made additive-only, tile size setting removed — landed 2026-08-22.** (1) The "saved states" Settings section is gone; `#saveStateBtn`/`#loadStateBtn` icons now sit on the Library page (left of the "Simplified names"/"Group by author" switches) — Save opens a name-prompt confirm modal, Load opens a `#loadStateDropdown` panel listing existing saves (name, mod count, timestamp) with per-row load/delete icon actions, closing on an outside click. Same `profiles.py` backend, no route changes. (2) Real bug fixed: `activate_profile()` used to also disable every mod *not* in the snapshot — reported as "the load feature does nothing" when a mod extracted via `broken_mods.py`'s repair action *after* a save was silently disabled again by loading that older save, with no visible explanation. Now additive-only: loading a state re-enables what it captured and leaves everything else alone, matching a "restore point" mental model instead of an exact-configuration switch — see `profiles.activate_profile()`'s docstring and `tests/test_profiles.py::test_regression_activate_profile_does_not_disable_mods_outside_the_snapshot`. (3) The Library tile-size (large/compact) Settings dropdown and `.tile-compact` CSS are removed entirely — cards are always the one standard size now, no per-user option.
- **Library auto-refresh via polling — landed 2026-08-22.** The backend's `Mods/` watcher already reacted to external filesystem changes on its own (~2s debounce, see "Startup scan"), but nothing told an already-open Library tab to re-fetch and show it — a manual edit was only ever reflected after a full page reload. `startLibraryPolling()` re-fetches the same data `loadMods()` already does (`/api/mods`, `/api/conflicts`, `/api/blacklist/matches`, `/api/mods/broken`, factored into a shared `fetchLibraryData()`) every `LIBRARY_POLL_INTERVAL_MS` (5s), skipping the tick entirely while the Library view isn't visible or a previous tick is still in flight, and only actually re-rendering when the fetched data differs from `state.librarySnapshot` (a plain `JSON.stringify` comparison) — so an idle library with nothing changing costs a fetch but never a redundant re-render (no disrupted scroll position, collapsed author groups, etc.). Deliberately plain polling rather than a WebSocket/SSE push, matching the same tradeoff already accepted for Assisted Mode's download detection (`startDownloadPolling()`) — a local single-user app doesn't need sub-second latency, and polling avoids a connection lifecycle to manage for no visible benefit. A failed poll tick fails silently (no error banner) since it's a background convenience, not a user-initiated action — the next tick or the next real action's own `loadMods()` resyncs regardless.
- **Regression, fixed 2026-08-22: a mod folder dropped in mid-session (e.g. a manual duplicate made outside SimsLink) stayed invisible until a full restart.** Root cause: of the three `scanner.py` scan paths, only the one-time startup scan ever called `import_untracked_mods()` — the real-time watcher's own debounced rescan (`schedule_mods_rescan`) and the manual "Full scan" button both re-hashed already-*tracked* mods only, never discovering brand-new real folders. Fixed by unifying the startup scan and the watcher's debounced rescan into one shared `run_mods_rescan()` (`import_untracked_mods()` then `incremental_scan()`), and by having `POST /api/settings/full-scan` call `import_untracked_mods()` first too (now reports `mods_imported` alongside its existing stats). See "Startup scan" above and `tests/test_backend_main.py::test_regression_schedule_mods_rescan_also_imports_untracked_mods` / `::test_full_scan_also_imports_untracked_mods`. What this does *not* cover, restart or not — out of scope for both scanners by design, not a remaining gap in this fix: a bare `.zip` sitting loose directly at the `Mods/` root (not inside a folder), and anything dropped inside an already-managed mod's own folder (a symlink — both scanners skip symlinks outright).
- **Installation paths (game folder, game user folder, library folder) editable from Settings — landed 2026-08-22.** New `backend/path_settings.py` + `settings` table (generic key/value): `SIMS4_GAME_DIR`/`SIMS4_USER_DIR`/`LIBRARY_DIR` in `.env` are now only the *bootstrap* — `main.py`'s `create_app()` layers any `settings`-table override on top of the `.env`-derived `Config` at startup (`apply_stored_overrides()`, before any route/watcher closure captures `config`), and `POST /api/settings/paths` lets the user change any subset of the three afterward. **Coherence check** (`validate_paths()`): hard-rejects (`PathValidationError` → HTTP 400) if the three paths aren't all distinct, or if `library_dir` is nested inside (or contains) `Mods/` — that specific case would create recursive symlinks; only warns (a code, e.g. `game_dir_no_executable`, mapped through i18n — never a raw sentence, unlike `PathValidationError`'s message, which follows this codebase's existing convention of passing exception text straight into the generic "Error: {error}" template) when the game folder doesn't contain a recognizable `TS4_x64.exe`/`TS4_DX9_x64.exe` under `Game/Bin/`, since Proton/Steam layouts vary too much to hard-block on this. **Migration, not a bare setting swap**: `mods.library_path` is an absolute path per mod, and every active mod has a real symlink under the *old* `Mods/` — silently repointing the setting would leave every mod's `library_path` stale and every symlink dangling, the same silent-breakage shape `migrate_legacy_data_dir()` (config.py) already had to clean up once. So `update_paths()` physically moves the library folder's contents (`shutil.move()` — same-filesystem rename when possible, only removes the source after a cross-filesystem copy fully succeeds, so a failure partway leaves the original untouched), rewrites every `mods.library_path` row, and re-creates every *active* mod's symlink through the new `Mods/` location (`mod_manager.activate_symlink()`/`deactivate_symlink()` — promoted from `_activate`/`_deactivate` to plain module functions specifically so `path_settings.py` could reuse the exact same symlink-creation logic instead of duplicating it). No extra whole-library backup on top of that move, since the move itself doesn't destroy the source until the destination is confirmed complete. **Still needs a restart**: the change to `config` takes effect immediately for every route (they all read the same closed-over `config`, reassigned via `nonlocal` right after a successful `POST /api/settings/paths`), but `scanner.ModsFolderWatcher` snapshots the directory it watches once, at `.start()` — it keeps watching the *old* `Mods/` location until the app restarts, the same "changed here, applies after restart" contract already used for `LOG_LEVEL`/`MODS_WATCHER_ENABLED`. Frontend: the Settings "Folders" section gets three text inputs (pre-filled, editable) plus a "Save paths" button, replacing what was a fully read-only row for those three; `mods_dir` (still purely derived, never independently settable) and `download_watch_dir` (out of scope here) stay as read-only rows underneath.
- **Loose files at Mods/ root: adoption, tagging, filter, and merge suggestions — landed 2026-08-22.** Investigating a real "SimsLink is missing most of my mods" report led to a much bigger-than-expected finding: 1194 `.package`/`.ts4script` files sitting directly at `Mods/` root, invisible to *both* existing scanners (`scanner.import_untracked_mods()`/`broken_mods.scan_broken_mods()` only ever look at *directories* under `Mods/`, explicitly skipping anything else). Confirmed via research this isn't an edge case — it's the officially-taught Sims 4 install method ("place .package/.ts4script files in Mods, no folder needed" — EA's own help article says exactly this), so this affects any library that predates SimsLink or mixes manual installs with app-managed ones, not just this one case.
  - New `backend/loose_mods.py`: `import_loose_files()` adopts every such file, **one file = one mod**, deliberately never guessing which loose files "really" belong together at import time (a wrong automatic grouping would be a one-way door — nothing in this app can split a merged mod back apart). Manual-only (`POST /api/settings/import-loose-files`, a Settings button, same reasoning as "Full scan") — never folded into the automatic watcher rescan, since a single new *file* appearing (unlike a whole folder) is ambiguous enough to need a deliberate trigger. Each import tags the new mod `is_loose_import` (`mods` table, see schema above) and removes the original loose file (left behind, the game would silently load the same content twice).
  - **Confirmable tidy-up, not automatic merging**: `suggest_groupings()` clusters still-`is_loose_import`-tagged mods by shared leading name segments (the backend counterpart to `app.js`'s inferred-author clustering, simplified — tries the longest shared prefix first, retries shorter ones on what's left over), surfaced read-only via `GET /api/mods/loose/suggested-groups` in a new Settings "Loose mods" section — each suggestion shows an editable name and every member, with **Merge**/**Ignore** per group. `merge_mods()` (`POST /api/mods/loose/merge`) backs up each source folder first, stages every file into one archive, installs it as a single new mod through the normal pipeline, then deletes the old ones — scoped to only `is_loose_import` mods (a tidy-up tool for this specific population, not a general "merge any two mods" feature). Ignoring a suggestion is session-only (no persisted dismiss list); a merged group just stops matching next time since its members are gone.
  - Frontend: a real mod card/row (not a synthetic pseudo-mod), so it goes through the normal grid/list/sort/grouping pipeline unchanged — it just also carries a neutral "Loose"/"Vrac" badge (`buildBadges()`, plain styling, not alarming — this isn't a defect) and shows up under a new `#looseOnlyFilterBtn` chip (Library head-controls, count-labeled, hidden entirely once there are none left) that filters the grid down to just these.
  - **Performance, found and fixed along the way**: importing found this via a real 1150-mod stress test — a single `render()` call cost ~105-154ms at that scale (WebKit-measured), enough to visibly hitch on every action *and* on every search keystroke (`wireSearch()` called `render()` synchronously per keystroke). Three independent fixes, all verified by re-measuring rather than assumed: (1) the search input is now debounced (150ms) so typing costs one render per pause, not one per character; (2) the grid's card-append loop now runs in ~8ms chunks across `requestAnimationFrame` calls (`renderGeneration` guards against two overlapping chunk runs if `render()` fires again before a previous pass finished) instead of one blocking loop, so a huge library "fills in" over a few frames instead of freezing for one; (3) `render()`'s sort comparator was calling `String.prototype.localeCompare(..., {sensitivity: "base"})` — which builds a throwaway `Intl.Collator` internally on *every* call — twice per comparison across an O(n log n) sort; replaced with one shared `BASE_COLLATOR` instance (`new Intl.Collator(...)`, reused via `.compare()`), which alone cut the sort step from ~24ms to a few ms at n=1150. Combined, a warm re-render at that scale dropped from ~105-154ms to ~14ms; a cold first render (page just loaded) to ~54ms.
- **"Mods you might want to reinstall" reminders — landed 2026-08-22.** Closes a gap raised while discussing what happens when a saved state ("Save/Load") goes stale: deleting a mod that was part of a save used to leave no trace at all — `profile_mods.mod_id` has `ON DELETE CASCADE`, so the membership row disappeared the instant the mod did, with nothing left to recover even at the next `activate_profile()` (a stale member can never be *seen* there for the same reason — the cascade already erased it by then, so a "detect it when the save is next loaded" backstop is structurally impossible, not just unimplemented). The only place this can be caught is *before* the deletion, while the mod's name/link are still readable — new `profiles.record_missing_mod_if_saved(mod_id, conn)` does exactly that, called from `main.py`'s `DELETE /api/mods/{mod_id}` route immediately before `mod_manager.delete()`. Deliberately **not** hooked into `mod_manager.delete()` itself: that function is also the first half of the delete-then-reinstall replace step `download_watcher.confirm_replace()` and `broken_mods.fix_rezipped_mod()` both use for updates/repairs, which isn't a real removal and must never produce a spurious reminder — see `tests/test_profiles.py::test_regression_replacing_a_saved_mod_does_not_create_a_missing_reminder`. No-ops when the mod isn't part of any saved state. New `missing_mods` table (id, mod_id, name, curseforge_url, source_profile_names, detected_date) — no FK to `mods`/`profiles`, both denormalized as plain text at detection time specifically so the reminder outlives the rows that produced it; unique on `mod_id` so reloading/re-deleting never duplicates an entry. `list_missing_mods()` is live-filtered (excludes any `mod_id` that's since reappeared, e.g. reinstalled under the same freed slug) rather than actively purged, so a resolved reminder can reappear untouched if the same mod goes missing again later, instead of a fresh duplicate being inserted. Routes: `GET /api/mods/missing`, `DELETE /api/mods/missing/{id}` (manual dismiss, no confirm modal — clears only a bookkeeping row, touches no real file). Frontend: a new purple `#missingModsBanner` (same collapsible pattern as the existing conflicts banner, but a separate section — a reminder with an optional reinstall link isn't the same *kind* of row as a broken-folder entry to delete, so they were kept visually distinct rather than merged into one list) shows each entry's name, which save(s) it came from, a "View on CurseForge" link when known, and a dismiss icon.
- **Editable namespace override for the Library's guessed grouping label — landed 2026-08-22.** [[Author-namespace terminology]]: the header a mod groups under (`frontend/app.js`'s `groupingAuthor()`) is frequently a *guessed* mod/series namespace — a bracket prefix, a shared-name-segments cluster — not a confirmed author, and the guess can be wrong. Migration 9 adds `mods.namespace_override` (see schema above), deliberately a separate column from `author` (real data, only ever populated from CurseForge) rather than overwriting it — this records "how I want this mod grouped in my Library," not a claim about who made it. `mod_manager.set_namespace_override(mod_id, value, conn=conn)` normalizes (`value=None` or blank clears it, reverting to normal author/prefix/cluster inference) behind `POST /api/mods/{mod_id}/namespace-override`. `groupingAuthor()` checks it first, ahead of `mod.author`/prefix/cluster inference — it exists specifically to override a wrong guess, including a real `mod.author` a user might want to group differently from CurseForge's own field. Frontend: every `.author-group-header` gets an edit icon (`buildAuthorHeader()`) opening a confirm modal pre-filled with the current label; saving applies the same override value to every mod currently grouped under that header in one batch (`doSetNamespaceOverride()`, `Promise.all` over the group's members) rather than one mod at a time, since the label is shared by the whole group. Only the grouping label changes — `displayName`/version stay driven by the mod's actual name either way.
- **Detects a mod manually rezipped in place — landed 2026-08-22.** Closes the other gap found during the same manual testing as the regression above: `Mods/<mod_id>/` is a symlink straight into the mod's real library folder, so replacing its extracted content with a `.zip` directly there (e.g. "dezip a mod via the app, then rezip it manually") landed in the library folder itself with nothing ever noticing — out of scope for both `scanner.py` (only tracks `.package`/`.ts4script`, silently drops the rest from `mod_files`) and `broken_mods.py` (only looks at *unmanaged* folders, explicitly skips anything that's a known mod id or a symlink). New `broken_mods.scan_rezipped_mods()` checks each tracked mod's real library folder live (not `mod_files`, which only catches up after the next scan) for "zero loadable files, at least one `.zip`" and reports it separately from the unmanaged-folder population; `fix_rezipped_mod()` handles the unambiguous single-archive case by staging a copy of the zip, backing up the folder, then reusing `mod_manager.delete()`+`install()` — the same delete-then-reinstall shape `download_watcher.confirm_replace()` already uses for updates — so a correct `mod_files`/`primary_type` comes out of the normal install pipeline rather than hand-rolled extraction; the mod keeps its id in the common case since deleting the old row frees the slug back up. Routes: `GET /api/mods/rezipped`, `POST /api/mods/{mod_id}/fix-rezip`. Frontend: a rezipped mod is a **real** mod card/row (not a synthetic pseudo-mod like a broken folder), so it goes through the normal grid/list/sort/grouping pipeline unchanged — it just also gets the same purple "Zip" badge and `is-archive` border/background an unmanaged `unextracted_archive` folder gets, sorts into the same top "confirmed non-functional" tier as a broken folder (`modTier()`), and — only when there's exactly one archive to disambiguate, same restraint as `fix_broken_mod()` — an extra unzip icon button next to its normal enable/disable-toggle and delete actions (`buildRezipActionButton()`), plus the equivalent one-click action in its detail panel (`#dBrokenActions`, shown alongside the normal Open-folder/Delete buttons rather than replacing them, unlike a true broken folder's detail view). More than one zip present gets flagged with no action button, matching `extract_selected_zips()`'s ambiguity — not extended to this case since a manually-rezipped *tracked* mod realistically only ever has the one archive.
- **"Keep this one" for duplicate/conflict comparisons — landed 2026-08-23.** Frontend-only, no new backend route: the side-by-side comparison `renderDetailConflicts()` already showed for any conflict group (all four `conflict_detector.py` kinds already read "Pick which one to keep below.") only ever offered per-side Disable/Delete, with nothing that acted on the *other* side(s) for you. `buildConflictSide()` now takes the group as a second (optional — omitted for the single-mod blacklist case, which has no "others") argument and, when present, adds a "Keep this one"/"Garder celui-ci" button alongside the existing two. Clicking it opens a confirm modal (radio choice, reusing `.zip-choice-list`/`.zip-choice` styling) asking whether the *other* mod(s) in that comparison should be disabled (default, reversible) or deleted, then loops the existing per-mod `/api/mods/{id}/disable`/`DELETE /api/mods/{id}` routes over them (`doResolveConflictGroup()`, same partial-failure-collection shape as `doUpdateAll()`) — never picks delete on the user's behalf, and never forces a choice at all, since the original per-side buttons are left in place for manual resolution. Successfully disabling at least one mod triggers the same cache-cleanup suggestion `toggleActive()`'s disable path already does. The currently open detail mod is always either the kept mod or one of the resolved others (the group only ever contains groups the open mod itself belongs to), so afterward it's reopened if it still exists (kept, or only disabled) or closed if it was deleted — same pattern `doDelete()` already used for the plain per-side delete button.
- **Crash Mode's page removed, replaced by two topbar popup triggers; type-filter counts; search-clear button; bulk-clear custom groupings — landed 2026-08-23.** Frontend-only, no backend routes changed.
  - **Crash Mode is no longer a nav page/view.** `#view-crash` and its `nav.crash_mode` sidebar entry are gone; `VIEWS` in app.js drops `"crash"`. In its place, two always-visible buttons sit in the topbar (`.topbar-actions`, next to the mode pill): "My game crashed"/"Mon jeu a planté" (`#headerCrashButton`) and "Clear cache"/"Effacer le cache" (`#headerClearCacheButton`) — reachable from any view, not just one dedicated page. Clicking "My game crashed" opens a new popup (`#crashOverlay`, styled as a wider `.confirm-modal--wide` variant with its own close button, `openCrashModal()`) and immediately starts analysis — no separate "I crashed" click needed inside it anymore. `#crashStatus`/`#crashErrorBanner` kept their ids and every bit of existing crash-analysis/bisection JS logic (`doAnalyzeCrash()`, `renderCrashReportBlock()`, `startBisection()`, etc.) unchanged; only the chrome around them moved from a page into a modal. "Clear cache" already opened its own confirm modal before this change (`clickClearCache()`/`doClearCache()`, unchanged) — it just moved from a page-header button to a global one. New wrinkle this surfaced: with both actions reachable before any modal is open, an error from the *first* step of "Clear cache" (fetching `/api/cache/targets`, which fails before `openConfirmModal()` ever runs) had nowhere sensible to render — `crashErrorBanner` lives inside a crash popup that isn't necessarily open. Fixed with a small floating `#headerErrorBanner` (`.topbar-actions-wrap`, absolutely positioned under the header buttons, same pattern as `.state-dropdown`) that `clickClearCache()`/`doClearCache()` now use instead; `crashErrorBanner` stays reserved for errors during crash analysis/bisection, which only ever happen once that popup is already open.
  - **Type-filter switch now shows live counts.** The CC/Mix/Scripts switch's three labels get a live `" (N)"` suffix — `renderTypeFilterCounts()`, called at the end of `render()`, counts `state.mods` by `primary_type` ("Mix" gets the full library count, matching what clicking it — the no-filter position — would show). These three buttons dropped their `data-i18n` attributes in index.html since `renderTypeFilterCounts()` is now their sole text owner (always runs after any `applyStaticI18n()` call, since `render()` always follows it).
  - **Search input gets a clear ("✕") button.** `#searchClearBtn`, absolutely positioned inside `.search-wrap` (mirrors the existing search icon on the left), hidden until the input has text; `wireSearch()` toggles its visibility on every keystroke and clears both the input and `state.filterQuery` on click. Scoped to the Library's `#searchInput` only, not the Catalog's separate CurseForge search box.
  - **Settings gets a "Clear custom groupings" bulk action.** New section (below "Loose mods") bulk-clears `mods.namespace_override` for every mod that has one — the other half of the existing per-group edit icon (`confirmEditNamespace()`/`doSetNamespaceOverride()`), for undoing a library-wide reorganization in one click instead of editing each group header individually. `confirmClearNamespaceOverrides()` counts affected mods from `state.mods`, opens a confirm modal, and `doClearNamespaceOverrides()` loops the same `POST /api/mods/{id}/namespace-override` route with `value: null` — no new backend route, same shape as `doSetNamespaceOverride()`.
- **Duplicate handling reworked into a multi-panel comparison; detail panel icon-ified with a sticky action bar and scrollable file list; collapsible-banner chevrons; cache-target list detail; header button icons — landed 2026-08-23.**
  - **Duplicate comparison, replacing the in-panel mini widget.** The cramped `.conflict-sides` comparison this session's own earlier "Keep this one" addition lived in is gone for a real duplicate match (`exact_duplicate_mod`/`folder_duplication` — conflict_detector.py's two strongest signals; weaker ones, `duplicate_package`/`ts4script_name_collision`/blacklist, are unaffected and keep the plain per-side Disable/Delete `buildConflictSide()` always had). Clicking a card/row that belongs to such a group (`findDuplicateGroupForMod()` — checked by group *membership*, not just which side carries the visible "Duplicate"/"Identical content" tag, so either mod in a pair opens the same flow) now asks up front, before anything opens: `promptDedup()` (reusing the generic `openConfirmModal()`, its new optional `onCancel` — see below) shows the Disable/Delete choice (radio, defaults to disable) alongside "compare these mods?". **Cancel** opens the plain single `#detail` panel for just the clicked mod, same as any other mod — nothing here is mandatory, same "suspicion is not confirmation" spirit as elsewhere. **Confirm** opens `openDuplicateComparison(modId, action)`: one full detail-style panel per mod in the group (`buildComparePanelNode()` — same content as the single `#detail` panel: hero, name/author/badges, description, dependencies, scrollable files — built into its own subtree since several render at once, not the fixed `#detail` ids), laid out edge-to-edge in `#dupCompareRow` with the clicked mod's own panel anchored rightmost (where `#detail` would normally sit) and every other member glued to its left (`.dup-compare-row`, `justify-content: flex-end`, scrolled to its far right on render for the rare 5+-member case) — with "click a panel to keep it, resolve the rest" selection mode already armed for the chosen action (`state.dupSelectMode`), no second in-view popup. **Deliberately no `.detail-actions` icon row on these panels** (no open-folder/toggle/delete) — this view exists purely to pick one of the mods, so the whole panel is the only "action" there is, for as long as the comparison stays open (`renderDupSelectHint()`'s pill is purely instructional now, no disarm button — there's nothing left to fall back to without those buttons). Clicking any panel (`resolveDuplicateSelection()`) disables/deletes every *other* mod in the group via the shared `applyActionToMods()` helper (partial-failure-collecting, same shape `doUpdateAll()` already used) and closes the view. `#overlay` is shared between the single panel and this view; `closeAnyDetail()` (its new `onclick`) closes whichever is actually open, and `openDetail()`/`openDuplicateComparison()`/`openBrokenDetail()` defensively close the other one first so they can never be open simultaneously. `openConfirmModal()`'s new `onCancel` param (module-level `confirmOnCancel`, invoked by a new `cancelConfirm()` — the Cancel button's and the backdrop-click's handler now, `closeConfirm()` itself stays cancel-agnostic since `onConfirm` handlers call it directly) is optional and every pre-existing call site is unaffected.
  - **Single detail panel: icon-only sticky action bar, files get their own scrollbox.** `#dActions` (Open folder/Disable-Enable/Delete, plus a repair/re-extract icon where applicable) moved outside the newly-added `.detail-scroll` wrapper — `.detail` is now a flex column with `.detail-scroll` as the only scrolling child, so the actions stay reachable with zero scrolling regardless of how long the description/dependencies get, closing a real usability gap on a mod with a huge file list. All four actions are icon buttons now (`.detail-icon-btn`, a bigger bordered variant of the card-level `.icon-btn`) instead of full-width text buttons, harmonizing with the card/row preview's own icon actions; `#toggleActiveBtn` (new — the detail panel previously had no enable/disable control of its own, unlike a card's `.toggle` switch) reuses `toggleActive()` and the existing `resolve_disable_button`/`resolve_enable_button` i18n strings as its title. `#dFiles` gets `.detail-files-scroll` (`max-height: 260px; overflow-y: auto`) so a mod with thousands of files no longer dominates the panel's own scroll height. `renderBrokenDetail()`'s reason-specific repair action and delete button are icon-ified the same way, reusing `brokenActionSpec()`'s existing `icon`/`iconBtnClass` fields (previously only used for the card-level compact button).
  - **Collapsible-banner chevrons.** The conflicts/missing-mods banner toggles (`#conflictsToggle`/`#missingModsToggle`) previously set plain `textContent` (with a static leading emoji baked into the i18n string) with no visual indication of expand/collapse state. `setCollapseToggleContent()` rebuilds each as `[chevron, text]` — same rotating-chevron language `buildAuthorHeader()`'s author-group collapse already used (`CHEVRON_ICON_SVG`, `.collapsed` rotates -90deg) — called from `renderWarnings()`/`renderMissingMods()` instead of a raw `textContent` assignment.
  - **Cache-cleanup target list: real names, extensions visible, open-location icon.** `buildCacheTargetNodes()` previously folded each target into one dash-joined `"{name} — {description}"` line; now each gets its own row (`.cache-target-row`) with the on-disk name (verbatim, extension included when there is one — e.g. `localthumbcache.package`, monospace like a `.file-row`) on its own line above the description, plus an open-location icon button. New `POST /api/cache/targets/{name}/open` (main.py) validates `name` against `cache_cleaner.list_cache_targets()`'s fixed spec before ever joining it onto `sims4_user_dir` (same reasoning as `open_folder()` only ever opening a mod's own `library_path`) — `xdg-open`s the target itself if it's a directory, or its parent (`sims4_user_dir`) if it's a file, since launching a single file's associated app isn't useful the way revealing it in a file manager is. `doOpenCacheTarget()` in app.js calls it; failures go to the same `#headerErrorBanner` `clickClearCache()`/`doClearCache()` already use.
  - **Header button icons.** "My game crashed"/"Clear cache" (`#headerCrashButton`/`#headerClearCacheButton`) get a leading SVG icon (alert-triangle, trash-can — same paths as `BROKEN_ICON_SVG`/`DELETE_ICON_SVG`) — moved their label into a nested `<span data-i18n="...">` (same pattern the Save/Load state buttons already used) since a bare `data-i18n` on the button itself would let `applyStaticI18n()`'s `textContent` assignment wipe the icon out.
- **Warnings banner ("X file(s) to clean up") gets an open icon, visible sample files, and a session dismiss — landed 2026-08-23.** Corrects a mistargeted fix from earlier this session — "the list of files to clean up" the missing open-icon/extension request actually referred to is this banner's `.conflict-row` list (`library.conflicts.toggle`'s literal wording), not the unrelated Clear-cache confirm modal, which already got its own (separate, still valid) open-icon treatment above. Each row previously showed only the *folder* name (`scan_broken_mods()`'s `empty`/`unrecognized` population — real directories under `Mods/`, sample_files never surfaced here) with no way to see what was actually inside or open it. Now: `folder.sample_files` (extensions included, e.g. `SCCO_Log.log`) renders as a monospace sub-line under the folder name (`+N…` past the sampled cap, same pattern the broken-detail panel already used), and a new open-location icon calls `POST /api/mods/broken/{name}/open` (main.py — resolves `name` against `sims4_mods_dir`, same authorization-by-existence-check `delete_broken_folder()` already relies on, then `xdg-open`s the folder itself) via `doOpenBrokenFolder()`. Also new: a small `✕` (`#conflictsDismissBtn`, next to the existing expand/collapse toggle in a new `.conflicts-banner-head` row) hides the whole banner for the session (`state.conflictsBannerDismissed`) without touching any of the underlying folders — it reappears on next launch regardless, and *within* the same session too the instant the list is genuinely emptied by fixing/deleting every entry (so a later, unrelated problem is never silently suppressed by a stale dismissal).
- **"Open mods folder" header button; native OS folder picker in Settings — landed 2026-08-23.** A third header button (`#headerOpenModsFolderButton`, next to "My game crashed"/"Clear cache") reveals `SIMS4_MODS_DIR` in the file manager — `POST /api/settings/open-mods-folder` (main.py, `config.sims4_mods_dir` — a live property, so this always reflects whatever path-settings currently has configured) via `doOpenModsFolder()`, same `xdg-open` "reveal in file manager" pattern as every other open-location action in this app. Settings' three path fields (game/user/library folder) each get a new browse button opening a **native** OS folder-picker dialog instead of only free-typing the path — `POST /api/settings/pick-folder` (payload: optional `initial_dir`) shells out to `zenity --file-selection --directory` (bundled with most GTK desktops, matching this project's WebKitGTK/GTK stack) and returns the chosen absolute path, or `{"path": null}` on cancel; a missing `zenity` binary is a 501, not a crash. This has to be a **backend** call — CLAUDE.md's "Desktop shell temporarily browser-only" entry is exactly why: a plain `<input type="file" webkitdirectory>` in a browser tab can't resolve a chosen folder to a real absolute filesystem path (browsers deliberately don't expose that), so there's no way to do this from the frontend alone until pywebview's own file-dialog bridge is back in play. `doBrowseFolder(inputId)` in app.js calls the route with the field's current value as `initial_dir` (a sensible starting point, not a requirement) and fills the input on success; typing the path directly still works exactly as before, this is purely a shortcut. All three new routes are best-effort/Linux-only, consistent with every existing `xdg-open` call in this file.
- **Broken/unzip/duplicate summary banners, plus a bulk resolve for each where it's actually safe to — landed 2026-08-23.** Frontend-only, no backend routes changed. Three more collapsible, session-dismissible banners join "Fichiers à nettoyer" in the Library head, for populations that already get card/badge treatment in the grid but had no aggregate count/jump-to summary — a real gap once a library reaches the thousands and the relevant card can be scrolled well out of view. Deliberately **not** merged into the existing `conflictsBanner` (kept that already-working markup/CSS untouched) — instead a new shared `renderBanner()`/`wireBanner()` (`.warning-list-banner`/`.warning-list-toggle`/`.warning-list-body` in style.css, themeable via a `--danger`/`--purple`/`--warn` modifier class) generalizes the same collapsible/session-dismiss pattern `conflictsBanner` established, so the interaction only needed to be built right once. This does knowingly reintroduce the "double-surfacing" the original `conflictsBanner` comment explicitly avoided for `unpacked_script`/`unextracted_archive` (they already get their own red/purple pseudo-mod card) — accepted here because a banner now does something a card can't: aggregate count at a glance regardless of scroll position, and (where safe) a bulk action across every qualifying entry at once.
  - **"Mods cassés"** (`#brokenModsBanner`, red/`--danger`): `unpacked_script` folders only (`empty`/`unrecognized` stay in the original `conflictsBanner` — a different, non-"broken" category). Each row: name, `sample_files` sub-line (same extension-visible treatment `conflictsBanner` just got), an open-location icon, the reason-specific repair icon (`buildBrokenActionButton()`), and delete. No bulk action — `attempt_script_repair()` is explicitly best-effort/"can fail silently" (CLAUDE.md's own broken_mods.py notes), too risky to fire unattended across every entry.
  - **"Mods à dézipper"** (`#unzipBanner`, purple/`--purple`): combines two different backend concepts that read the same to a user — an unmanaged folder with an unextracted zip (`unextracted_archive`) and an already-tracked mod manually rezipped in place (`state.rezippedMods`, `scan_rezipped_mods()`) — since both just mean "go unzip this." **"Unzip all" bulk button** (`unzipBulkEligible()`) — unlike the broken-mods repair above, this *is* safe to bulk-apply: it only ever fires on entries with exactly one archive, the same unambiguous case `brokenActionSpec()`/`buildRezipActionButton()` already gate their individual one-click actions on (2+ zips has no safe guess at which is "the" content and is simply excluded from the count/action, not attempted). Loops `POST /api/mods/broken/{name}/fix` / `POST /api/mods/{mod_id}/fix-rezip` with `applyActionToMods()`-style partial-failure collection.
  - **"Mods en double"** (`#duplicatesBanner`, orange/`--warn`): one row per `exact_duplicate_mod`/`folder_duplication` group (member names joined with "↔"), a **Compare** button that opens the exact same `promptDedup()` flow a card click does. **"Resolve unambiguous" bulk button** — the one place bulk auto-resolution is genuinely defensible: it only ever acts on `duplicateTagModIds()`'s existing set (the "presumed redundant" side — already computed from a real signal, an author/no-author split or a name's trailing "(1)/(2)"), reusing `applyActionToMods()` directly on that id list. A group with **no** such signal (both sides equally unauthored/unnamed-distinctly — the common case in Assisted Mode, confirmed against this project's real library: 28/28 current `exact_duplicate_mod` groups have no author on either side, so the button shows "Resolve unambiguous (0)", disabled, with a title explaining why) is never auto-picked — same "suspicion is not confirmation" rule the rest of this app already follows for exactly this reason (CLAUDE.md's duplicate-tagging notes on not risking "a wrong, unfounded accusation"). Those stay comparison-only via the per-group Compare button.
  - `buildDisableDeleteChoice()` factors the disable/delete radio pair out of `promptDedup()` (now shared with the duplicates bulk-resolve confirm) — same markup/behavior, one definition.
- **Old per-side conflict widget fully retired for a real-duplicate mod's own detail view — landed 2026-08-23.** `renderDetailConflicts()`'s previous fix (excluding just the `exact_duplicate_mod`/`folder_duplication` *kinds*) left a real gap: a mod in one of those groups can independently share a weaker signal (`duplicate_package`/`ts4script_name_collision`) with a *third*, unrelated mod — conflict_detector.py's precedence only suppresses a weaker signal for a pair *already covered* by a stronger one, not for every signal touching a mod that happens to be a duplicate elsewhere. Confirmed against this project's real library: "accouchement réaliste" (an `exact_duplicate_mod` pair) also shares a `duplicate_package` overlap with a third mod, so opening its single detail (via the pre-comparator popup's Cancel, since retired — see below) still showed the old Disable/Delete comparison widget for that other pairing — reading, to a user, exactly like "the old dedup tool" hadn't actually gone anywhere. Now: `modConflicts` is `[]` outright whenever `findDuplicateGroupForMod(mod.id)` is truthy — a real-duplicate mod's own detail view shows *no* conflict-group widget at all anymore, full stop; that mod's dedup story is entirely the multi-panel comparator's job now. Blacklist matches are unaffected (separate feature, not a dedup tool). The *other* mod in that weaker pairing (the third, non-duplicate one) is untouched — opening *its* detail still shows the `duplicate_package` conflict from its own perspective, since suppressing real information for an unrelated mod was never the goal.
- **Pre-comparator popup removed; the comparator now deletes by default — landed 2026-08-24.** `promptDedup()` (the "disable or delete the others?" confirm modal shown before the duplicate comparator opened) is gone — clicking a duplicate-tagged card/row, or a duplicates-banner "Compare" button, now calls `openDuplicateComparison(modId)` directly with no upfront choice. Selection mode is always armed for **delete**: clicking a panel keeps that mod and deletes every other member of the group (`resolveDuplicateSelection()` hardcodes `"delete"`, no longer reads an `action` from `state.dupSelectMode`, now a plain boolean instead of `{ action }`). `renderDupSelectHint()` shows one fixed hint (`library.detail.dedup_select_hint`) instead of switching between a disable/delete variant. `buildDisableDeleteChoice()`/its `dedup_confirm.action_disable`/`action_delete` i18n keys are unaffected — still shared by the unrelated "Resolve unambiguous" bulk action in the duplicates banner, which keeps its own disable/delete choice. Removed now-dead i18n: `dedup_confirm.title`/`dedup_confirm.confirm`, `dedup_select_hint_disable` (renamed `dedup_select_hint_delete` → `dedup_select_hint`), and `conflict_exact_duplicate_summary`/`conflict_folder_duplication_summary` (only ever used by the removed popup's message, now that the old per-side widget is fully retired for these two kinds — see above).
- **CurseForge fingerprint matching for loose imports; "Tracked" badge; card/row thumbnails — landed 2026-08-23.** Once a real API key existed (see above), a throwaway exploration script validated CurseForge's file-fingerprint API (the same mechanism the official CurseForge app uses to recognize a file dropped in by hand — a 32-bit MurmurHash2, seed 1, over the file with whitespace bytes stripped; platform-wide, not Minecraft-specific) directly against this project's real library: every exact match correctly identified both the right mod *and* the right file variant (e.g. the right fan translation) — see the transcript for the concrete before/after examples. A second exploration specifically tested whether this could "reconstruct" mods shattered into many loose files (1145/1253 of this real library are `is_loose_import`) by clustering matches on a shared CurseForge modId — that hypothesis didn't pan out (406/1066 tested loose files matched, but 0 mod ids were shared by 2+ of them — this library's loose files are mostly genuinely distinct small CC items, not fragments of one bigger download), so reconstruction was dropped; the identification signal itself (406/1066 = 38%) was strong enough to build for real on its own.
  - **`backend/curseforge.py`** gains `curseforge_fingerprint(bytes) -> int` (the hash itself — `struct.unpack_from` for the 4-byte-word extraction, still a Python-level loop for the actual mixing, which is why a size cap exists downstream) and two `CurseForgeClient` methods: `match_fingerprints(fingerprints) -> {fingerprint: modId}` (exact matches only — partial matches are dropped entirely, same "suspicion is not confirmation" rule as everywhere else; correlates via the response's own top-level `exactFingerprints` array, *not* `match.file.fileFingerprint`, which doesn't reliably equal what was sent — verified directly against the real API) and `get_mods(modIds) -> list[CurseForgeMod]` (bulk `POST /v1/mods`, one request for every distinct match instead of one `get_mod()` per match).
  - **New `backend/curseforge_match.py`**: scoped deliberately to `is_loose_import = 1 AND curseforge_id IS NULL` mods only (not every unlinked mod), since a loose import is reliably single-file and a multi-file mod folder has no non-arbitrary "representative file" to pick. Fingerprints each candidate's one file (skipping anything over 25MB — a handful of already-merged multi-hundred-MB `.package` files in this real library would otherwise dominate the whole run for no benefit, since an already-merged file isn't a realistic loose-CC candidate anyway); for each match fills in `curseforge_id`/`author`/`thumbnail_url`/`links` via `COALESCE` — never overwrites a field something else already set.
  - **Chunked and resumable, not one big blocking call — reworked 2026-08-23** to back a live progress popup instead of Settings' original "button shows its own running state and waits" pattern (Full scan/Import loose files' style — kept for those, since neither has per-item progress worth showing). `MatchSession` (`remaining`/`total`/`checked`/`matched`/`skipped_too_large`) lives in `app.state` (single in-progress run at a time, fine for a local single-user app); `start_session(conn)` builds the candidate list fresh every time (a mod matched by an earlier, possibly-stopped-early run already has `curseforge_id` set by then, so it's naturally excluded — no separate "resume" bookkeeping needed), `run_step(session, conn, client, chunk_size=CHUNK_SIZE=20)` processes one chunk and commits its matches immediately. Routes: `POST /api/settings/match-curseforge/start` and `.../step` (both Direct Mode only, via `require_client()`), returning `{total, checked, matched, skipped_too_large, done}` each time. Stopping a run is purely a **frontend** decision (the loop just stops calling `.../step`) — nothing already matched is ever lost, since every step's writes are already durable by the time its response comes back.
  - **Header button + progress popup, replacing the old Settings entry — landed 2026-08-23.** "Tenter d'identifier les mods"/"Try to identify mods" (`#headerMatchCurseforgeButton`, magnifying-glass icon, next to the other three header buttons but — unlike them — only shown when `direct_mode` is true, same toggle `renderStatus()` already used for the Catalog nav entry) opens `#matchCurseforgeOverlay`: a progress bar + "`{checked} / {total} checked — {matched} identified`" line, refreshing the Library (`loadMods()`/`render()`) after every chunk so a "Tracked" badge/thumbnail that just landed shows up while the popup is still open. `openMatchCurseforgeModal()` calls `.../start` then loops `runMatchCurseforgeStep()` (`.../step`) until `done` or `state.matchCurseforgeRunning` is set false. The one bottom button doubles as **Stop** (while running — `setMatchCurseforgeButtonMode()`) and **Close** (once done or stopped) rather than two separate buttons, since only one is ever relevant. Verified against this project's real 1145-loose-mod library — `POST .../start` correctly reported `739` remaining candidates (1145 total minus the 406 this feature had already linked when the user tried the original Settings button before this rework, all independently confirmed correct — e.g. `adeepindigo-...-traduction-fr-par-kimikosoma...` → `author: "KimikoSoma"`, real `curseforge_id`, real thumbnail).
  - **"Suivi"/"Tracked" badge**: a mod with `compat_status` stuck at `"unknown"` (no version-range data — the norm without full catalog metadata) but a real `curseforge_id` now shows "Suivi" instead of "Inconnue" — `compatDisplayKey(mod)`, used everywhere `compatKey()` used to be. Distinguishes "the app knows exactly which mod this is, just not its compatible version range" from genuinely knowing nothing.
  - **Thumbnails, and a bigger image/body ratio for them — landed/tuned 2026-08-23.** `mod.thumbnail_url` was already returned by `GET /api/mods` but nothing in the frontend ever rendered it — cards/rows always showed the letter-avatar placeholder regardless. Now an `<img>` (`.thumb-image`/`.avatar-image`, absolutely positioned to fill the existing thumb/avatar box) is shown when present, falling back to the same letter-avatar (`buildThumbInitial()`/`fillAvatarInitial()`, factored out so the fallback and the image's own `onerror` handler build the exact same thing) on a failed load instead of a broken-image icon. Grid card's `.thumb` grew from 38% to 46% of the card's own height (a real thumbnail reads much better with more room) — `.card-body p`'s description dropped from a 2-line to a 1-line clamp to free up the space, keeping everything still fitting within the card's `aspect-ratio: 1/1` square even at the grid's smallest column width (230px, ~8px of slack either way — this was tuned, not guessed, since the old 38%/2-line combination was already using nearly all the room the smallest card has). List row's `.avatar` grew from 40px to 56px — no rebalancing needed there, an unconstrained flex row just grows a bit taller.
  - `mod_summary()` (main.py) now also returns `curseforge_id` (was already computing `thumbnail_url`, just never used) — needed by the frontend to tell "Tracked" apart from genuinely unknown.
- **CurseForge matching robustness fix — landed 2026-08-23.** Reported after real use on this project's own library ("it just stops on its own", "restarted from zero"): `run_step()`'s original implementation popped a chunk off `session.remaining` (and, for the file-hashing part, could raise `OSError` outright) *before* the two CurseForge API calls — so a single transient failure (network hiccup, a slow/rate-limited response, a missing/unreadable file) both crashed the whole popup (uncaught exception -> 500, frontend gave up entirely) *and* silently dropped that chunk's mods from the session's own tracking, without ever processing them — not permanently lost (they're still `curseforge_id IS NULL` in the DB, so a *fresh* `/start` would eventually re-offer them), but enough to make an in-progress run's own counters desync, which is what read as "restarted from zero" even though no `/start` had actually been called again.
  - **`run_step()` now leaves `session` completely untouched until the risky calls succeed** — `session.remaining`/`session.checked` only advance at the very end, after `match_fingerprints()`/`get_mods()` already returned successfully; a per-mod file-read failure (`OSError` — deleted mid-run, permission hiccup, a broken symlink) is caught individually and just skips that one mod instead of failing the whole chunk. A caller re-running `run_step()` with the same `session` after a `CurseForgeError`/`requests.RequestException` therefore retries the *exact same* chunk — never skips it, never double-counts it.
  - **main.py's `.../step` route** catches that and returns **502** (distinguishing it from a real client error) — except `curseforge.CurseForgeAuthError` specifically (the key itself rejected mid-run, e.g. revoked/expired), which is **401** and clears the session outright, since retrying a bad key can't ever succeed.
  - **Frontend retries automatically** on a 502 — `runMatchCurseforgeStep()` now takes a `retryCount`, backs off `2s × attempt` (up to `MATCH_CURSEFORGE_MAX_RETRIES = 4`) while showing "CurseForge didn't respond — retrying (N/4)…", and only gives up (shows the error, switches the button to Close) after exhausting retries or hitting a non-502 error. `apiRequest()` now attaches the HTTP status to the `Error` it throws (`err.status`) specifically so a caller can make this distinction — every other existing caller is unaffected, none of them read it.
  - 4 new regression tests (`test_curseforge_match.py`, `test_backend_main.py`) covering: a failed `run_step()` leaves `remaining`/`checked`/the DB completely untouched; a retried call after a transient failure processes the *same* chunk correctly; the route returns 502 (retryable, session intact) vs 401 (auth failure, session cleared).
- **"Suivi"/"Tracked" renamed to "Lié"/"Linked"; a filter chip for it — landed 2026-08-23.** Renaming: more precise (nothing about this is actually monitored on an ongoing basis, the mod is just linked to a real CurseForge entry) and echoes the app's own name. `compatDisplayKey()` now returns `"linked"` instead of `"tracked"`, `library.compat.tracked` is now `library.compat.linked` — applied everywhere this badge is used, nowhere else (SimsLink's own generic "tracked mod"/"tracked files" terminology elsewhere in the codebase, e.g. loose_mods.py's own vocabulary, is unrelated and untouched). New `#linkedOnlyFilterBtn` (`renderLinkedFilterChip()`/`wireLinkedFilterChip()`) filters the grid down to mods with a real `curseforge_id` — same "Loose"/"Translation" chip pattern (`.loose-filter-chip`, session-only, hidden entirely once there are none), same underlying signal as the badge.
- **Fixed: a fingerprint match never filled in short_description/category — landed 2026-08-23.** Reported as "descriptions and dependencies don't show for linked mods" — the description half was a real bug: `_apply_match()` only ever set `curseforge_id`/`author`/`thumbnail_url`/`links`, never `short_description`/`category`, even though both come free in the same bulk `get_mods()` response that found the match (no extra request). `full_description` deliberately still isn't fetched — `CurseForgeClient` only exposes it via `get_description(mod_id)`, a *separate* per-mod request that would add one more API call per newly-matched mod on top of what a chunk already needs. Applied retroactively too: a one-time backfill script re-fetched `get_mods()` for the 479 mods this project's own real library had already matched *before* the fix and re-ran `_apply_match()` (idempotent/COALESCE-safe) on each — 0 left missing afterward.
  - **Dependencies not showing is expected, not a bug** — *superseded 2026-08-24, see the "CurseForge-declared dependencies" entry further down: `curseforge_dependencies.py` now parses CurseForge's own per-file `dependencies` data into this app's `dependencies` table, exactly the feature this note used to flag as unbuilt.*
- **Data-integrity incident: wrong `curseforge_id` values from a real, reproducible bug in `match_fingerprints()` — found, root-caused, and fixed 2026-08-23.** Raised while investigating whether linked loose mods could be "restructured"/merged by shared `curseforge_id` (the reconstruction angle above says didn't pan out pre-launch, 0/406) — with more of the library since matched (479 by this point), the same query found 57 real collisions: 2+ loose mods sharing one `curseforge_id` despite being visibly unrelated (different authors, different file sizes) — e.g. `herasims-minimal-plant-3`/`lumpinou-moodpackmod-v1-677` both stored as `700870`.
  - **First pass (incomplete)**: reset every mod caught in a same-`curseforge_id` collision back to unlinked (349 stayed linked, 796 went back to the unlinked pool), and could not reproduce the mis-pairing under live testing at the time — logged as "most likely stale data from the pre-fix concurrency bug, not conclusively proven." That conclusion was wrong: re-checking after the user resumed the "Try to identify mods" popup, **20 more mods got linked and produced brand-new collisions**, including one sharing modId `896574` — independently already confirmed to be a completely different, unrelated mod ("Minimal plants #3 - set"). The bug was still live.
  - **Actual root cause, confirmed by capturing the raw API response**: `match_fingerprints()` paired the response's top-level `exactFingerprints` with `exactMatches` positionally (`zip(exactFingerprints, exactMatches)`), assuming both arrays line up 1:1 in request order. They don't — `exactFingerprints` is simply an *echo of every fingerprint sent*, matched or not (always the same length as the request), while `exactMatches` has one entry **per matched file**, and a single CurseForge file can bundle more than one of the fingerprints in a request (its `.package` and its `.ts4script`, when the same original download got split into two separate local loose imports — a common case). Whenever that happens, `exactMatches` is shorter than `exactFingerprints`, and the naive positional zip silently shifts every pairing after that point — reproduced live and directly: in one real 20-item chunk, a single bundled file (`kayladot-npcevents-shared` + `-2`, both legitimately the same CurseForge file) desynced all 12 subsequent items in that chunk, producing 20/44 wrong assignments across a handful of test chunks. Not every misassignment collides visibly with another mod — some land on an id nothing else claims — which is why the first pass's collision-only cleanup missed a large share of the damage.
  - **Fix**: `match_fingerprints()` now correlates by *content*, not position — for each `exactMatches` entry, every fingerprint in that file's own `modules[].fingerprint` list is checked against what was actually sent, independent of array length/order/how many sent fingerprints one matched file accounts for. A fingerprint claimed by two different matches (shouldn't legitimately happen) is dropped rather than guessed at. See `tests/test_curseforge.py`'s `test_match_fingerprints_one_file_bundling_two_sent_fingerprints` (the exact `kayladot`-style regression) and `test_match_fingerprints_drops_a_fingerprint_claimed_by_two_different_matches`. Re-verified against the real API on the exact chunk that previously mismatched: now reproduces the raw response exactly (the bundled pair both correctly resolve to the same modId, every other item gets its own correct, distinct id).
  - **Full reset, not another selective cleanup**: given the first pass's collision-only approach provably missed non-colliding corruption, every loose mod's `curseforge_id`/`author`/`category`/`short_description`/`thumbnail_url`/`links` was reset back to unlinked — not just the colliding ones. All 1145 loose mods are back to unlinked, ready to be re-matched from scratch through the now-fixed `POST /api/settings/match-curseforge/step`.
  - **Restructuring loose mods by shared `curseforge_id`** (`loose_mods.suggest_groupings()`'s new curseforge-confirmed clustering — see below) is now safe to rely on, now that the actual bug producing wrong ids is fixed rather than papered over.
- **`suggest_groupings()`/`merge_mods()` gain a confirmed-identity signal via shared `curseforge_id` — landed 2026-08-23.** Answers the "can we restructure linked loose mods" question the incident above was raised while investigating. `GroupSuggestion` gains an optional `curseforge_id` field (`None` for the existing name-heuristic path). `suggest_groupings()` now clusters in two passes, strongest signal first: mods sharing the exact same `curseforge_id` (a confirmed identity, not a guess) are grouped and removed from consideration before the existing shared-leading-name-segments heuristic runs on whatever's left — so a mod is never suggested twice under two different signals, and a curseforge-confirmed group always sorts ahead of a heuristic one in the returned list. `merge_mods()`: when every source in a merge shares the same `curseforge_id`, the new merged mod inherits it plus the `author`/`category`/`short_description`/`thumbnail_url`/`links` that came with it, so the result stays "Lié" instead of reverting to unlinked — a plain name-heuristic merge is unaffected, same blank-defaults behavior as before. No new route: `GET /api/mods/loose/suggested-groups` just adds `curseforge_id` to its existing response shape.
- **Mergeable-mods moved from Settings to a Library banner, with a real multi-panel comparator — landed 2026-08-23.** The inline Settings row (name input + Ignore/Merge) is gone; `loose_mods.suggest_groupings()`'s output is now part of the regular Library data cycle (`fetchLibraryData()`/`state.looseSuggestions`, alongside conflicts/broken mods/etc.) and surfaces as a new "Mods fusionnables" banner (`#mergeableBanner`, orange/`--warn`, same collapsible/session-dismissible `renderBanner()`/`wireBanner()` pattern as the broken/unzip/duplicates banners) — each row shows the member names, a green "Confirmed on CurseForge" pill when `curseforge_id` is set, and a **Comparer** button. No bulk action here, unlike duplicates' "resolve unambiguous": there's no safe unattended default for "which loose files should become one mod," confirmed-CurseForge or not — every group goes through the comparator.
  - **The comparator itself** (`openMergeComparison()`) reuses the exact same side-by-side multi-panel layout the duplicate comparator already established — full detail-style panels (thumbnail, description, dependencies, files) so the decision is made with real information, not a bare list of names — factored out into a shared `fillComparePanelBody(panel, mod, {topNode})` that both `buildComparePanelNode()` (duplicate, click-a-panel-to-keep) and `buildMergeComparePanelNode()` (merge, a checkbox per panel via `topNode`) build on. Unlike duplicates, merging combines an arbitrary *subset* of the group rather than picking one survivor, so every panel gets an include/exclude checkbox (all checked by default) instead of being a click target itself; a floating bar carries the editable merged name and "Fusionner (N sélectionnés)", disabled below 2 selections.
  - **Two real bugs found and fixed the same day, both from the same root lesson — a comparator's controls must never be nested inside the thing that scrolls/hovers:**
    1. The confirm bar was first built as an absolutely-positioned child *inside* the scrollable `#mergeCompareRow` — with enough panels to need horizontal scrolling, this could end up covered or unreachable. Fixed by making `#mergeCompareBar` a real `position: fixed` sibling of the row (same pattern `#dupCompareCloseBtn` already used, which is exactly why duplicates never had this problem) — `fillMergeCompareBar()` now populates a static element instead of building/appending a new one on every render.
    2. `.merge-panel:hover { background: none; }` (meant to cancel `.dup-panel:hover`'s highlight, since a merge panel isn't itself a click target) actually cleared the panel's own base background too, making it transparent on hover and letting whatever sat behind intercept clicks meant for its own checkbox. Fixed to restore `var(--panel)` (the panel's real base background) instead of `none`.
  - **Horizontal scroll via plain mouse wheel didn't work, for both comparators** — `.dup-compare-row` (shared by `#dupCompareRow`/`#mergeCompareRow`) already had `overflow-x: auto`, but the row's height exactly matches its panels' (100vh), so there's nothing to scroll *vertically* — an unmodified wheel event (no Shift, no trackpad horizontal swipe) did nothing at all, reading as "can't scroll" even though the row was technically scrollable via drag-the-scrollbar. Fixed with `wireHorizontalWheelScroll()`, wired once at init for both row ids: redirects a vertical-dominant wheel event into `scrollLeft`, leaving genuinely horizontal input (trackpad swipe) alone.
- **"Lié"/"Linked" text badge replaced by a green/red status dot on grid cards; CurseForge link surfaced in the detail panel(s); Escape closes the comparator/detail panel — landed 2026-08-23.**
  - `compatDisplayKey()` (the "linked" special case layered over `compatKey()`) is removed outright — every compat pill (grid/list badges, single detail panel's `#dStatus`) goes back to plain `compatKey(mod.compat_status)` (compatible/incompatible/unknown only). In its place, `buildCard()`'s `.card-meta-actions` row (left of the active/inactive toggle) gets a small `buildLinkStatusDot(mod)` — green (`.is-linked`) when `mod.curseforge_id` is set, red otherwise, with a tooltip. Grid-only, per request — list rows and the detail panel simply lost the old text badge with nothing added in its place there.
  - **CurseForge link, previously nowhere in the app despite being stored**: `mods.links`'s `curseforge_url` was already parsed server-side (`mod_curseforge_url()`, used by the Updates checklist) but never returned by `GET /api/mods/{id}` — `get_mod()` now includes it (`curseforge_url`, `None` when unlinked; deliberately not added to `mod_summary()`/the bulk list endpoint, single-mod detail only). Frontend: `#dCurseforgeLink` (a `<button class="pill detail-curseforge-link">`, navigation via the existing `openExternal()`/`POST /api/open-external` — opens in the system's default browser, never the app's own window, same as every other external link in this app) shown in the single detail panel whenever `mod.curseforge_url` is set, hidden for a broken-folder pseudo-mod. `fillComparePanelBody()` (both comparators) gets the same button when present, with `stopPropagation()` since a duplicate-comparator panel is itself a click target.
  - **Escape key** closes whichever of the duplicate comparator / merge comparator / single detail panel is currently open — one global `keydown` listener reusing `closeAnyDetail()` (already had the right three-way branch logic for the mouse-driven close paths). Deliberately scoped to just these three per the request — doesn't touch the confirm/crash/match-curseforge modals, which use their own separate overlay elements.
- **CurseForge-declared dependencies for linked mods, plus a small dependency graph in the detail panel — landed 2026-08-24.** Closes the gap CLAUDE.md itself used to flag as unbuilt (see the old "Dependencies not showing is expected, not a bug" note, now superseded by this entry).
  - **A real gap found and fixed along the way**: `dependencies.check_required()` blocked `mod_manager.enable()` on any unresolved `dependency_type='required'` row regardless of `confidence` — every dependency-creating path before this only ever produced `confidence='confirmed'` rows, so this was never exercised, but the new CurseForge-derived rows are `confidence='suggested'` by design (never auto-confirmed, same as translation detection). Without the fix, the very first auto-suggested required dependency to a not-yet-active mod would have silently blocked `enable()` before the user ever agreed to anything. Now filters `missing` to `confidence == "confirmed"` before raising `UnresolvedRequiredDependencyError` — see `tests/test_dependencies.py::test_regression_check_required_ignores_suggested_dependencies`.
  - **New `backend/curseforge_dependencies.py`**, the Direct-Mode-only bridge module (mirrors `curseforge_match.py`'s role, keeping `dependencies.py` itself mode-independent per its own docstring). `fetch_and_suggest_dependencies(mod_id, *, client, conn)`: for a linked mod, fetches its CurseForge mod's `main_file_id` (new field on `CurseForgeMod`, from `mainFileId`) via the new `CurseForgeClient.get_file(mod_id, file_id)` (single-file fetch, symmetric with existing `get_files()`), and for each declared dependency (`CurseForgeFile.dependencies`, new field parsed from the file response's `dependencies[]`) that resolves to an already-installed local mod (`mods.curseforge_id` match), creates a `confidence='suggested'` row — `RequiredDependency`(3)→`required`, `OptionalDependency`(2)→`optional`; `EmbeddedLibrary`/`Incompatible`/`Tool`/`Include` are out of scope (not "needs this other mod installed" relations) and silently skipped, same for a dependency modId with no local match (skipped rather than stored as an unresolved "unknown mod" placeholder — no name to show for it without a second network round-trip). Idempotent: skips a `(depends_on_mod_id, dependency_type)` pair already stored, confirmed or suggested. Unlike translation detection's two-step detect→pick→suggest (needed there because a translation signal is inherently ambiguous), this writes its suggestions directly in one step — a CurseForge-declared dependency names an exact modId, nothing left to disambiguate once resolved locally. Route: `POST /api/mods/{mod_id}/detect-curseforge-dependencies` (400 if unlinked or Assisted Mode, 502 on a CurseForge API error). Verified against the real API/library: `midnitetech-career-pharmacist` correctly resolved a real declared dependency on the locally-installed `midnitetech_modlibrary`.
  - **Dependency graph, `renderDependencyGraph()` in app.js**: a small inline SVG above the existing `#dDependencies` `.dep-row` list in the single detail panel (not a literal `<canvas>` — the app has zero canvas usage and every existing visual is inline SVG styled via `stroke="currentColor"` + CSS `color`, same idiom reused here) — the mod's own node at top, one node per dependency below (wrapped into rows of 3), connected by an edge. Node/edge color encodes `dependency_type` (`--accent` required, `--text-dim` optional, `--purple` translation, matching `library.detail.dependency_type.*`), a dashed border/edge encodes `confidence === 'suggested'`. Clicking a node scrolls to and briefly highlights (`.dep-row-highlight`, cleared via `setTimeout`) its corresponding `.dep-row` below rather than duplicating Confirm/Reject inside the SVG — keeps that tested logic in exactly one place. A small legend row (`.dep-graph-legend`) explains the three type colors. Hidden entirely when the mod has no dependencies. New `#detectCurseforgeDependenciesBtn` next to the existing "Check for translation match" button, shown only for a linked mod (`mod.curseforge_id`) in Direct Mode.
- **Regression, found and fixed 2026-08-24 while researching CurseForge's versioning model: `game_version_min`/`game_version_max` were computed lexicographically, not numerically.** `_parse_file()` used to reduce a file's `gameVersions` (e.g. `["1.99", "1.100", "1.101"]`) via plain `min()`/`max()` over the raw strings — `"1.100"` sorts *before* `"1.99"` as text the moment the game crosses double into triple digits, so `game_version_min`/`game_version_max` (and downstream, `compat_status()`'s badge — which itself already does a real numeric comparison, it just received the wrong inputs) could be silently wrong for any mod whose compatible range spans that boundary. Fixed with the new `_min_max_game_versions()`, sorting by `_version_tuple()` (the same numeric parser `compat_status()` already trusts) with a lexicographic fallback only if an entry doesn't parse as a clean dotted-int version at all (defensive — CurseForge's `gameVersions` for this game has always been plain numbers so far, but nothing guarantees that forever). Checked against the real library: no installed mod currently has a stored `game_version_min`/`game_version_max` (Catalog installs are the only path that ever populates it, and every mod installed so far came in via Assisted Mode/loose import instead) — so this is a forward-looking fix, nothing to backfill. See `tests/test_curseforge.py::test_regression_game_version_range_is_numeric_not_lexicographic` and `::test_get_files_falls_back_to_lexicographic_on_unparseable_game_version`.
- **Two other version-related gaps found during the same research, deliberately not fixed yet — revisit if asked**: (1) `installed_version` — the field the detail panel/card show as "the mod's version" — is actually populated with `str(file_id)`, CurseForge's internal numeric file id (`build_metadata()` in main.py), not any human-meaningful version; CurseForge's API has no separate structured "creator's own version" field at all, only free text usually embedded in `fileName`/`displayName`. (2) The `mods.latest_version` column is declared in the schema but never written or read anywhere — fully dead. (3) `CurseForgeFile.release_type` (release/beta/alpha) is parsed but never surfaced in the UI. None of these are bugs — `installed_version` still works correctly as an update-detection signal (comparing file ids) — just gaps in what's shown to the user; fixing them well needs a real design decision (parse a version out of the filename heuristically? show the file name instead? show a release-date instead?), not a mechanical patch.
- **"Incompatible" badge + Library filter chip — landed 2026-08-24.** Closes a real gap found while investigating why the badge never showed up in practice: `compat_status` was already computed correctly (`compat_status()`, `_min_max_game_versions()` — see the regression above), but nothing in `buildBadges()` ever rendered it — fully dead data on the frontend, not a restore of something removed. Now: a mod with `compat_status === 'incompatible'` gets a `warn-badge` (orange — a caution, not confirmed-broken red, since a declared game-version range can be wrong/overly conservative) "Incompatible" pill on its card/row, right after the duplicate badge. New `#incompatibleOnlyFilterBtn` in the Library's `.head-controls` (same session-only, count-labeled, hidden-when-zero pattern as the existing Loose/Translation/Linked chips — `renderIncompatibleFilterChip()`/`wireIncompatibleFilterChip()`, filtered in `visibleMods()`). `mod_summary()` already returned `compat_status` on every `GET /api/mods` entry, so no backend change was needed here — purely a frontend fix.
- **`curseforge_dependencies.py` gains a bulk "Synchronize" sync + compat_status refresh — landed 2026-08-24.** Investigating the "Incompatible" badge above surfaced the real reason it had nothing to show: `compat_status` is *only* ever computed at Catalog-install time (`install_from_catalog()` in main.py) — never for a mod linked later via `curseforge_match.py`'s fingerprint matching, which is how this whole library got linked (Assisted Mode/loose import, then matched afterward). So `compat_status` stayed `'unknown'` for all ~570 real linked mods, and the per-mod "Check CurseForge dependencies" button already fetched the exact CurseForge file response (`game_version_min`/`game_version_max`) needed to fix this, just never used it for anything beyond dependencies.
  - `fetch_and_suggest_dependencies()` gains an optional `game_version: str | None = None` param (default keeps every existing call/test unaffected) — when given, it now also updates `mods.compat_status`/`game_version_min`/`game_version_max` via the new `_apply_game_compat()`, reusing the *same* `get_file()` response already fetched for dependencies, no extra API call. The single-mod detail-panel route (`POST /api/mods/{id}/detect-curseforge-dependencies`) now passes `config.game_version`, so that button already does double duty.
  - **New bulk sync**, same chunked/resumable/progress-popup shape as `curseforge_match.py`'s own bulk fingerprint-match run, but scoped to every *already-linked* mod (`WHERE curseforge_id IS NOT NULL`, not just loose imports) and re-run in full every time (no "last synced" bookkeeping, same simplicity `curseforge_match.start_session()` already accepts). `SyncSession`/`start_sync_session()`/`run_sync_step()` (`SYNC_CHUNK_SIZE = 10`, smaller than match's 20 — each mod costs two *sequential* API calls here, `get_mod()` then `get_file()`, not one batched pair per whole chunk). Differs from `curseforge_match.run_step()`'s all-or-nothing chunk retry: since calls are inherently per-mod (not batched), a single mod's transient failure (`CurseForgeError`/`requests.RequestException`) is caught and skipped individually (counted in `session.errors`, that mod just stays stale for a later run) rather than failing the whole chunk — only a `CurseForgeAuthError` (key rejected) is non-retryable and propagates out, failing the whole run (same as match's handling). Routes: `POST /api/settings/sync-curseforge/start` / `.../step`.
  - **Frontend**: new header button "Synchroniser"/"Synchronize" (`#headerSyncCurseforgeButton`, Direct Mode only, next to "Tenter d'identifier les mods") opens `#syncCurseforgeOverlay` — same popup shell/behavior as the match-curseforge one (`openSyncCurseforgeModal()`/`runSyncCurseforgeStep()`/`closeSyncCurseforgeModal()`, the one bottom button doubling as Stop/Close), reusing its generic "Stop"/"Close" i18n strings rather than duplicating them. Simpler step-loop than match-curseforge's, though: no client-side 502-retry-with-backoff dance, since `run_sync_step()` already absorbs per-mod transient failures server-side — only a 401 (key rejected) ends the run early.
  - Verified against the real API/library: `POST .../start` correctly found 534 already-linked mods; three real steps (30 mods) all synced with zero errors, and `compat_status` came back genuinely populated for the first time ever — several older fan-translation mods correctly flagged `'incompatible'` against the real installed game version (1.126.78.1020).
